---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .node-version
  - .eleventy.js
  - .gitignore
  - .editorconfig
  - src/_data/site.json
  - src/_includes/layouts/base.njk
  - netlify.toml
  - LICENSE
autonomous: false
requirements: [INFRA-01]
user_setup: []

must_haves:
  truths:
    - "A fresh Eleventy 3.1.6 project builds an empty _site with `npm run build`"
    - "The dependency list is confirmed by a human before the first npm install (slopcheck could not run)"
    - "Netlify build config and a tightened CSP exist before any content"
  artifacts:
    - path: "package.json"
      provides: "type:module, devDependencies, and the full prebuild/build/test/a11y script chain from research"
      contains: "prebuild"
    - path: ".eleventy.js"
      provides: "DEBT filters findBy, jsonScript, number, isoDate, readableDate; src/_site/_includes/_data dir config"
      contains: "findBy"
    - path: "netlify.toml"
      provides: "Netlify build command + tightened CSP and security headers, NODE_VERSION 24"
      contains: "Content-Security-Policy"
    - path: "src/_includes/layouts/base.njk"
      provides: "Accessible semantic base layout, lang en-GB, skip link, no information by colour alone"
  key_links:
    - from: "netlify.toml"
      to: "package.json"
      via: "command = npm run build runs the prebuild gate chain"
      pattern: "npm run build"
---

<objective>
Stand up a fresh Eleventy 3.1.6 project mirroring the DEBT blueprint conventions, install the minimal accepted dependency set behind a human confirmation gate, and lay down the Netlify build config with a tightened CSP. This is the substrate every later wave builds on. No content, no gates yet, just the toolchain and a buildable empty site.

Purpose: Every gate needs a host from day one (INFRA-01). The project must build before schemas, gates, or rendering can be added.
Output: A buildable Eleventy project with package.json scripts wired for the four gates (scripts created later), a base layout, site.json, netlify.toml CSP, MIT LICENSE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@./CLAUDE.md

<!-- DEBT analogs to mirror verbatim where the pattern fits: -->
@/Users/anthonygeorge/Projects/DEBT/.eleventy.js
@/Users/anthonygeorge/Projects/DEBT/package.json
@/Users/anthonygeorge/Projects/DEBT/netlify.toml
@/Users/anthonygeorge/Projects/DEBT/.node-version
@/Users/anthonygeorge/Projects/DEBT/src/_data/site.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the dependency list before first npm install</name>
  <read_first>
    - 01-RESEARCH.md section "## Package Legitimacy Audit" (slopcheck unavailable; all six packages corroborated by DEBT use or STACK.md, no postinstall scripts)
    - 01-RESEARCH.md section "## Standard Stack" (exact versions)
  </read_first>
  <what-built>The exact dependency set to be installed: @11ty/eleventy@3.1.6 (runtime build), and devDependencies ajv@8.20.0, ajv-formats@3.0.1, pa11y-ci@4.1.1, start-server-and-test@3.0.11, http-server@14.1.1. All build-time only; the site ships as static HTML. None carries a postinstall script.</what-built>
  <how-to-verify>
    1. Confirm each package name and version against npmjs.com (npmjs.com/package/ajv, npmjs.com/package/pa11y-ci, etc.).
    2. Confirm no extra packages are added beyond these six (Ajv is the one deliberate non-default per research; do not add others).
    3. Approve to proceed to install.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or list any package to remove or substitute.</resume-signal>
  <acceptance_criteria>Human has explicitly approved the six-package list before any `npm install` runs. This satisfies the slopcheck-substitute gate (threat T-01-SC).</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Scaffold the Eleventy project mirroring DEBT and install dependencies</name>
  <files>package.json, package-lock.json, .node-version, .eleventy.js, .gitignore, .editorconfig, src/_data/site.json, src/_includes/layouts/base.njk</files>
  <read_first>
    - /Users/anthonygeorge/Projects/DEBT/.eleventy.js (the filters to mirror: findBy, jsonScript, number, isoDate, readableDate; dir config input src, output _site, includes _includes, data _data; markdown/html template engine njk)
    - /Users/anthonygeorge/Projects/DEBT/src/_data/site.json (shape to adapt: title, tagline, description, lang en-GB, nav array)
    - 01-RESEARCH.md section "### `prebuild` wiring (package.json)" (the exact scripts block: validate, lint:editorial, check:images, prebuild, build, test, a11y:serve, a11y:ci, a11y:all)
    - 01-RESEARCH.md section "### Recommended Project Structure"
  </read_first>
  <action>
    Create package.json with "type": "module", author and "license": "MIT", an empty (to be filled) dependency on @11ty/eleventy at 3.1.6 in devDependencies, and the full scripts block exactly as research specifies: validate runs node scripts/validate-data.mjs, lint:editorial runs node scripts/check-editorial.mjs, check:images runs node scripts/check-images.mjs, prebuild chains validate then lint:editorial then check:images, build runs eleventy, test runs node --test 'test/**/*.test.js', a11y:serve runs http-server _site -p 8081 -s -c-1, a11y:ci runs pa11y-ci, a11y:all chains build then start-server-and-test. Do NOT use npx in the scripts (DEBT uses npx; install locally instead so CI is deterministic). The scripts/* and test/* targets are created in later plans; package.json declares them now.
    Create .node-version containing "24" (mirror DEBT). Create .eleventy.js mirroring DEBT's filters findBy, jsonScript, number, isoDate, readableDate and the same dir/templateFormats config; drop DEBT's poundsShort filter (not relevant here). Create .gitignore (node_modules, _site, .DS_Store, .pa11y artefacts) and .editorconfig (mirror DEBT). Create src/_data/site.json with title "Food Transparency UK", a neutral plain-English tagline and description, "lang": "en-GB", and a placeholder nav (Home, Methodology) sized for Phase 1 only. Create src/_includes/layouts/base.njk: semantic HTML5 (header, main with id, footer), lang from site.lang, a visible skip link to main, viewport meta, mobile-first, no information conveyed by colour alone, visible focus not suppressed. Then install the six packages from Task 1 using --save-exact for eleventy and --save-dev for the rest, generating package-lock.json.
    Write all British English, no em-dashes (the editorial gate will later scan src/).
  </action>
  <verify>
    <automated>npx @11ty/eleventy && test -d _site && node --version</automated>
  </verify>
  <acceptance_criteria>`npx @11ty/eleventy` exits 0 and produces a _site directory (the full `npm run build` chain runs the prebuild gate scripts, which do not exist until Wave 4 — Plans 01-05/01-06 — so Wave 1 builds Eleventy directly; the end-to-end `npm run build` with all gates is verified in Plan 01-07 Task 2). package-lock.json exists and pins the six packages. .eleventy.js exports findBy and readableDate filters.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Netlify build config with tightened CSP and the MIT code licence</name>
  <files>netlify.toml, LICENSE</files>
  <read_first>
    - /Users/anthonygeorge/Projects/DEBT/netlify.toml (the headers block to mirror, including X-Content-Type-Options, Referrer-Policy, X-Frame-Options, Permissions-Policy)
    - 01-RESEARCH.md section "## Security Domain" CSP note (the exact tightened CSP string that drops the Cloudflare allowances)
  </read_first>
  <action>
    Create netlify.toml mirroring DEBT: build command "npm run build", publish "_site", NODE_VERSION "24". Mirror the DEBT security headers (X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, X-Frame-Options DENY, Permissions-Policy geolocation/microphone/camera/interest-cohort empty) but replace DEBT's CSP with the tightened Phase 1 CSP from the research Security Domain section: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; object-src 'none'; upgrade-insecure-requests. There is no analytics in Phase 1, so the Cloudflare connect-src and script-src allowances are deliberately omitted. Create LICENSE as MIT (code licence; the dataset licence LICENSE-DATA is created in plan 01-02).
  </action>
  <verify>
    <automated>grep -q "frame-ancestors 'none'" netlify.toml && grep -q 'NODE_VERSION = "24"' netlify.toml && grep -qi "MIT" LICENSE</automated>
  </verify>
  <acceptance_criteria>netlify.toml declares the npm run build command, NODE_VERSION 24, the tightened CSP (no cloudflareinsights references), and the four security headers. LICENSE is MIT and covers code only.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to local build | Third-party package code enters the build (supply chain) |
| Build output to browser | Static HTML served under CSP and security headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (six packages) | mitigate | Blocking human-verify checkpoint (Task 1) before first install; slopcheck unavailable so the list is human-confirmed against npmjs.com; no postinstall scripts; --save-exact pins versions; package-lock.json committed |
| T-01-01 | Tampering | Clickjacking of served pages | mitigate | X-Frame-Options DENY + CSP frame-ancestors 'none' in netlify.toml |
| T-01-02 | Spoofing | MIME sniffing | mitigate | X-Content-Type-Options nosniff |
| T-01-03 | Information disclosure | Secrets in repo | accept | No secrets in Phase 1 (OFF keys are Phase 2); .gitignore excludes node_modules and build artefacts |
</threat_model>

<verification>
- `npm run build` (or `npx @11ty/eleventy` if gate scripts are not yet present) produces _site.
- netlify.toml CSP contains no cloudflareinsights allowances.
- The dependency checkpoint was approved before install (no install without it).
</verification>

<success_criteria>
A fresh Eleventy 3.1.6 project builds, the six accepted dependencies are installed and pinned in a committed lockfile, the base layout is accessible and en-GB, and the Netlify build config with a tightened CSP is in place.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-01-SUMMARY.md` when done.
</output>
