---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .node-version
  - .npmrc
  - .eleventy.js
  - .gitignore
  - .editorconfig
  - src/_data/site.json
  - src/_includes/layouts/base.njk
  - src/index.njk
  - netlify.toml
  - LICENSE
autonomous: false
requirements: [INFRA-01]
user_setup: []

must_haves:
  truths:
    - "A fresh Eleventy 3.1.6 project builds with `npx @11ty/eleventy`, producing _site from a placeholder index (Plan 01-07 replaces it) and copying src/assets through"
    - "The full resolved dependency tree (not just the six direct names) is confirmed by a human before the first npm install (slopcheck could not run); the committed lockfile is the audited artefact"
    - "Netlify build config exists with a tightened CSP, HSTS, and a build script chained so the gates are not fail-open"
  artifacts:
    - path: "package.json"
      provides: "type:module, devDependencies, the full prebuild/build/test/a11y script chain from research, and build chained as `npm run prebuild && eleventy`"
      contains: "prebuild"
    - path: ".npmrc"
      provides: "save-exact=true to pin all six deps plus a dependency cooldown; does NOT set ignore-scripts (puppeteer needs its install script)"
      contains: "save-exact"
    - path: ".eleventy.js"
      provides: "DEBT filters findBy, jsonScript, number, isoDate, readableDate; src/_site/_includes/_data dir config; addPassthroughCopy of src/assets so pages ship styled"
      contains: "addPassthroughCopy"
    - path: "netlify.toml"
      provides: "Netlify build command + tightened CSP, HSTS, and security headers, NODE_VERSION 24"
      contains: "Content-Security-Policy"
    - path: "src/_includes/layouts/base.njk"
      provides: "Accessible semantic base layout trimmed to the Phase 1 site.json shape, lang en-GB, skip link, the styles.css link, no inline script, no information by colour alone"
  key_links:
    - from: "netlify.toml"
      to: "package.json"
      via: "command = npm run build runs the prebuild gate chain"
      pattern: "npm run build"
---

<objective>
Stand up a fresh Eleventy 3.1.6 project mirroring the DEBT blueprint conventions, install the minimal accepted dependency set behind a human confirmation gate, and lay down the Netlify build config with a tightened CSP. This is the substrate every later wave builds on. No content gates yet, just the toolchain, the assets passthrough, and a buildable site with a placeholder index.

Purpose: Every gate needs a host from day one (INFRA-01). The project must build before schemas, gates, or rendering can be added.
Output: A buildable Eleventy project with package.json scripts wired for the four logical gates across three prebuild scripts (scripts created later), a trimmed base layout, site.json, a placeholder index, .npmrc, netlify.toml CSP, MIT LICENSE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@./CLAUDE.md

<!-- DEBT analogs to mirror where the pattern fits (trim where Phase 1 has no matching data): -->
@/Users/anthonygeorge/Projects/DEBT/.eleventy.js
@/Users/anthonygeorge/Projects/DEBT/package.json
@/Users/anthonygeorge/Projects/DEBT/netlify.toml
@/Users/anthonygeorge/Projects/DEBT/.node-version
@/Users/anthonygeorge/Projects/DEBT/src/_data/site.json
@/Users/anthonygeorge/Projects/DEBT/src/_includes/layouts/base.njk
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the dependency list and the resolved tree before first npm install</name>
  <read_first>
    - 01-RESEARCH.md section "## Package Legitimacy Audit" (slopcheck unavailable; the six DIRECT packages corroborated by DEBT use or STACK.md; the "no postinstall" claim holds for the six direct deps ONLY, since pa11y-ci -> pa11y -> puppeteer runs an install-time Chromium download)
    - 01-RESEARCH.md section "## Standard Stack" (exact versions)
  </read_first>
  <what-built>The exact direct dependency set to be installed: @11ty/eleventy@3.1.6 (build), and devDependencies ajv@8.20.0, ajv-formats@3.0.1, pa11y-ci@4.1.1, start-server-and-test@3.0.11, http-server@14.1.1. All build-time only; the site ships as static HTML. None of the six DIRECT packages carries a postinstall script, BUT the transitive chain pa11y-ci -> pa11y -> puppeteer runs an install-time Chromium download (expected, not slop). The audited artefact is the full resolved tree in the committed lockfile, not just the six names.</what-built>
  <how-to-verify>
    1. Confirm each direct package name and version against npmjs.com (npmjs.com/package/ajv, npmjs.com/package/pa11y-ci, etc.).
    2. Confirm no extra direct packages are added beyond these six (Ajv is the one deliberate non-default per research; do not add others).
    3. After install, review the full resolved tree (`npm ls --all`) and the committed package-lock.json, not just the six direct names. Acknowledge puppeteer's Chromium download as the one expected install-time script.
    4. Confirm package-lock.json is committed before CI runs.
    5. Approve to proceed.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or list any package to remove or substitute.</resume-signal>
  <acceptance_criteria>Human has explicitly approved the six-package direct list AND reviewed the resolved tree / committed lockfile (acknowledging puppeteer's Chromium download) before relying on the install. This satisfies the slopcheck-substitute gate (threat T-01-SC).</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Scaffold the Eleventy project mirroring DEBT and install dependencies</name>
  <files>package.json, package-lock.json, .node-version, .npmrc, .eleventy.js, .gitignore, .editorconfig, src/_data/site.json, src/_includes/layouts/base.njk, src/index.njk</files>
  <read_first>
    - /Users/anthonygeorge/Projects/DEBT/.eleventy.js (the filters to mirror: findBy, jsonScript, number, isoDate, readableDate; the addPassthroughCopy for src/assets; dir config input src, output _site, includes _includes, data _data; markdown/html template engine njk)
    - /Users/anthonygeorge/Projects/DEBT/src/_data/site.json (shape to adapt: title, tagline, description, lang en-GB, nav array)
    - /Users/anthonygeorge/Projects/DEBT/src/_includes/layouts/base.njk (the layout to mirror BUT trim hard to the Phase 1 site.json shape; DEBT carries OG/Twitter/JSON-LD/analytics/sitemap/manifest/favicon/nav.js and meta.footerNote that Phase 1 has no data or files for)
    - 01-RESEARCH.md section "### `prebuild` wiring (package.json)" (the exact scripts block: validate, lint:editorial, check:images, prebuild, build, test, a11y:serve, a11y:ci, a11y:all)
    - 01-RESEARCH.md section "### Recommended Project Structure"
  </read_first>
  <action>
    Create package.json with "type": "module", author and "license": "MIT", a --save-exact dependency on @11ty/eleventy at 3.1.6, and the full scripts block exactly as research specifies: validate runs node scripts/validate-data.mjs, lint:editorial runs node scripts/check-editorial.mjs, check:images runs node scripts/check-images.mjs, prebuild chains validate then lint:editorial then check:images, build runs "npm run prebuild && eleventy" (explicit chain as defence in depth so the gates are not fail-open; the npm lifecycle also fires prebuild, but any direct `eleventy` / `npx @11ty/eleventy` invocation bypasses the gates, so CI plus this explicit chain plus prebuild give belt-and-braces), test runs node --test 'test/**/*.test.js', a11y:serve runs http-server _site -p 8081 -s -c-1, a11y:ci runs pa11y-ci, a11y:all chains build then start-server-and-test. Do NOT use npx in the scripts. The scripts/* and test/* targets are created in later plans; package.json declares them now.
    Create .node-version containing "24" (mirror DEBT). Create .npmrc with `save-exact=true` (so all six deps pin, not just eleventy) and a dependency cooldown — `min-release-age` if the installed npm supports it, otherwise a committed comment documenting Renovate `minimumReleaseAge` as the enforcement path (the cooldown bites at the next update, not the pinned initial install). Do NOT set `ignore-scripts` (that would skip puppeteer's required Chromium download and break the a11y floor).
    Create .eleventy.js mirroring DEBT's filters findBy, jsonScript, number, isoDate, readableDate and the same dir/templateFormats config, AND include `eleventyConfig.addPassthroughCopy({ "src/assets": "assets" })` so styles ship (omitting it would deploy every page unstyled). Drop DEBT's poundsShort filter and DEBT's favicon.ico / site.webmanifest passthroughs (those files do not exist in Phase 1). Create .gitignore (node_modules, _site, .DS_Store, .pa11y artefacts) and .editorconfig (mirror DEBT).
    Create src/_data/site.json with title "Food Transparency UK", a neutral plain-English tagline and description, "lang": "en-GB", and a placeholder nav (Home, Methodology) sized for Phase 1 only.
    Create src/_includes/layouts/base.njk: semantic HTML5 (header, main with id, footer), lang from site.lang, a visible skip link to main, viewport meta, mobile-first, no information conveyed by colour alone, visible focus not suppressed. Trim it to the Phase 1 site.json shape (title/tagline/description/lang/nav): DROP the OG/Twitter/JSON-LD/analytics/sitemap/manifest/favicon-svg/nav.js references and `meta.footerNote` until the matching data keys/files exist in later phases. The base layout must contain NO inline `<script>` and no inline event handlers (so the tightened `script-src 'self'` holds). Link the stylesheet as `<link rel="stylesheet" href="{{ '/assets/styles.css' | url }}">` (the file is authored in Plan 01-07; the passthrough copies it to _site/assets/styles.css; the link path is consistent across 01-01 and 01-07).
    Create src/index.njk as a trivial placeholder home page using base.njk (one heading, one line of neutral en-GB copy, a link to Methodology) so the build emits _site even before content; mark it clearly as a placeholder that Plan 01-07 replaces.
    Then install the six packages from Task 1 using --save-exact for eleventy and --save-dev for the rest, generating package-lock.json.
    Write all British English, no em-dashes (the editorial gate will later scan src/).
  </action>
  <verify>
    <automated>npx @11ty/eleventy && test -d _site && node -e "import('ajv/dist/2020').then(()=>import('ajv-formats')).then(()=>console.log('ajv import resolves')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>`npx @11ty/eleventy` exits 0 and produces a _site directory from the placeholder index (the full `npm run build` chain runs the prebuild gate scripts, which do not exist until Wave 4 — Plans 01-05/01-06 — so Wave 1 builds Eleventy directly; the end-to-end `npm run build` with all gates is verified in Plan 01-07 Task 2). The documented Ajv import specifier `ajv/dist/2020` (no extension) and the `ajv-formats` default import both resolve under Node 24 (finding-5 smoke). package-lock.json exists and pins the six packages; .npmrc sets save-exact and does NOT set ignore-scripts. .eleventy.js exports findBy and readableDate and registers the src/assets passthrough.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Netlify build config with tightened CSP, HSTS and the MIT code licence</name>
  <files>netlify.toml, LICENSE</files>
  <read_first>
    - /Users/anthonygeorge/Projects/DEBT/netlify.toml (the headers block to mirror, including X-Content-Type-Options, Referrer-Policy, X-Frame-Options, Permissions-Policy)
    - 01-RESEARCH.md section "## Security Domain" CSP note (the exact tightened CSP string that drops the Cloudflare allowances)
  </read_first>
  <action>
    Create netlify.toml mirroring DEBT: build command "npm run build", publish "_site", NODE_VERSION "24". Mirror the DEBT security headers (X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, X-Frame-Options DENY, Permissions-Policy geolocation/microphone/camera/interest-cohort empty) and ADD `Strict-Transport-Security = "max-age=31536000; includeSubDomains"`. Replace DEBT's CSP with the tightened Phase 1 CSP from the research Security Domain section, keeping script-src 'self', object-src 'none', base-uri 'self', frame-ancestors 'none' (these are correct): default-src 'self'; script-src 'self'; img-src 'self' data:; font-src 'self'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; object-src 'none'; upgrade-insecure-requests. For style-src, follow verify-then-tighten: the Phase 1 build emits no inline styles, so DROP `style-src 'unsafe-inline'` and set `style-src 'self'`; if a later inline style proves unavoidable, keep 'unsafe-inline' but record it as a known relaxation. There is no analytics in Phase 1, so the Cloudflare connect-src and script-src allowances are deliberately omitted. Create LICENSE as MIT (code licence; the dataset licence LICENSE-DATA is created in plan 01-02).
  </action>
  <verify>
    <automated>grep -q "frame-ancestors 'none'" netlify.toml && grep -q "Strict-Transport-Security" netlify.toml && grep -q 'NODE_VERSION = "24"' netlify.toml && grep -qi "MIT" LICENSE && ! grep -rqE "(<style|style=)" src/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>netlify.toml declares the npm run build command, NODE_VERSION 24, the tightened CSP (no cloudflareinsights references, script-src 'self', style-src 'self' since no inline styles are emitted), HSTS, and the security headers. The grep confirms src/ emits no inline `<style>`/`style=`. LICENSE is MIT and covers code only. The live deploy headers (HSTS + CSP) are confirmed in Plan 01-08 Task 3.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to local build | Third-party package code (incl. the transitive puppeteer Chromium download) enters the build (supply chain) |
| Build output to browser | Static HTML served under CSP, HSTS and security headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (six direct + resolved tree) | mitigate | Blocking human-verify checkpoint (Task 1) before relying on the install; slopcheck unavailable so the list AND the resolved tree / committed lockfile are human-confirmed against npmjs.com; "no postinstall" holds for the six direct deps, puppeteer's Chromium download acknowledged; .npmrc save-exact pins all six plus a cooldown; package-lock.json committed |
| T-01-01 | Tampering | Clickjacking of served pages | mitigate | X-Frame-Options DENY + CSP frame-ancestors 'none' in netlify.toml |
| T-01-02 | Spoofing | MIME sniffing | mitigate | X-Content-Type-Options nosniff |
| T-01-05 | Tampering | Transport downgrade / SSL strip | mitigate | Strict-Transport-Security max-age=31536000; includeSubDomains; upgrade-insecure-requests in CSP; confirmed on the live response in Plan 01-08 |
| T-01-03 | Information disclosure | Secrets in repo | accept | No secrets in Phase 1 (OFF keys are Phase 2); .gitignore excludes node_modules and build artefacts |
</threat_model>

<verification>
- `npx @11ty/eleventy` produces _site from the placeholder index; the Ajv import specifier resolves.
- netlify.toml CSP contains no cloudflareinsights allowances and includes HSTS.
- The dependency checkpoint was approved (list + resolved tree) before relying on install.
</verification>

<success_criteria>
A fresh Eleventy 3.1.6 project builds with the assets passthrough and a placeholder index, the six accepted dependencies are installed and pinned in a committed lockfile with .npmrc save-exact, the base layout is accessible, trimmed and en-GB, and the Netlify build config with a tightened CSP and HSTS is in place with a non-fail-open build chain.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-01-SUMMARY.md` when done.
</output>
