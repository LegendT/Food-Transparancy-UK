---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/_data/meta.json
  - schemas/source.schema.json
  - src/_data/sources.json
  - LICENSE-DATA
  - README.md
autonomous: true
requirements: [TRUST-02, TRUST-06, DATA-01, DATA-02, DATA-12]
user_setup: []

must_haves:
  truths:
    - "Confidence and evidence are two separate GRADE 4-point enums defined in meta.json"
    - "A source is recorded once with its licence, rights, jurisdiction and mandate-vs-incentive flag, and is cited by id"
    - "The canonical dataset declares an open licence so the archive can outlive its maintainer"
  artifacts:
    - path: "src/_data/meta.json"
      provides: "confidenceLevels + evidenceLevels (two GRADE enums) and datasetLicence"
      contains: "evidenceLevels"
    - path: "schemas/source.schema.json"
      provides: "Source registry record contract: id, name, publisher, url, covers, updateFrequency, retrievedDate, sourceType, jurisdiction, licence object, driver"
      contains: "sourceType"
    - path: "src/_data/sources.json"
      provides: "Seed registry incl. an Open Food Facts ODbL record and one GB-jurisdiction regulatory source"
      contains: "jurisdiction"
    - path: "LICENSE-DATA"
      provides: "Full open-licence text for the dataset (ODbL 1.0 default)"
  key_links:
    - from: "src/_data/sources.json"
      to: "schemas/source.schema.json"
      via: "every record validates against the source schema"
      pattern: "sourceType"
---

<objective>
Define the two-axis trust vocabulary (TRUST-02), the source/rights registry contract and seed (DATA-01/02, TRUST-06), and the open-data licence declaration (DATA-12). Source-type and licence/rights live on the source record, never on the fact. The mandate-vs-incentive driver flag is recorded now and enforced in Phase 4.

Purpose: A fact references a source by id; the registry is where licence obligations (ODbL share-alike), jurisdiction (GB vs EU separation), and source-type are made auditable.
Output: meta.json enums + datasetLicence, source.schema.json, a seed sources.json, LICENSE-DATA, and a README licence-split section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@./CLAUDE.md

<!-- DEBT analogs: -->
@/Users/anthonygeorge/Projects/DEBT/src/_data/meta.json
@/Users/anthonygeorge/Projects/DEBT/src/_data/sources.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-axis GRADE enums and dataset licence in meta.json</name>
  <files>src/_data/meta.json</files>
  <read_first>
    - /Users/anthonygeorge/Projects/DEBT/src/_data/meta.json (the single confidenceLevels block to mirror and extend to two blocks)
    - 01-RESEARCH.md section "### Pattern 2: Two-axis trust in `meta.json`" (the exact confidenceLevels and evidenceLevels definitions and datasetLicence)
    - REQUIREMENTS.md TRUST-02 (confidence = curator certainty; evidence = GRADE strength; both on a four-point High/Moderate/Low/Very Low scale; source-type lives in the registry, not here)
  </read_first>
  <action>
    Create src/_data/meta.json with two separate enum blocks. confidenceLevels: keys high, moderate, low, very-low, each with the plain-English definition of curator certainty that this record is correct and current. evidenceLevels: keys high, moderate, low, very-low, each with the GRADE definition of underlying scientific/regulatory evidence strength. Use the exact key set high/moderate/low/very-low (GRADE convention, symmetric across both axes) per the research Pattern 2 block. Add a datasetLicence object with id "ODbL-1.0" and url "https://opendatacommons.org/licenses/odbl/1-0/". Do NOT add any source-type field here (that belongs on the source record). Do NOT collapse the two axes into a single score. These confidenceLevels/evidenceLevels keys are kept in sync with the SourcedValue $defs.grade enum (plan 01-03), verified by the schema-consistency test in plan 01-04 (the wording is "kept in sync with, verified by test", not "draws from").
  </action>
  <verify>
    <automated>node -e "const m=require('./src/_data/meta.json'); const g=['high','moderate','low','very-low']; if(g.some(k=>!(k in m.confidenceLevels))||g.some(k=>!(k in m.evidenceLevels))) process.exit(1); if(!m.datasetLicence.id) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>meta.json has confidenceLevels and evidenceLevels each keyed by exactly high/moderate/low/very-low, and a datasetLicence.id. No source-type or combined trust-score field is present.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Source registry schema and seed records</name>
  <files>schemas/source.schema.json, src/_data/sources.json</files>
  <read_first>
    - /Users/anthonygeorge/Projects/DEBT/src/_data/sources.json (the base record shape: id, name, publisher, url, covers, updateFrequency)
    - 01-RESEARCH.md section "## Source / Rights Registry (DATA-01 / DATA-02 / TRUST-06)" (the extended record incl. retrievedDate, sourceType enum primary/secondary/tertiary/grey, jurisdiction enum GB/EU/international, licence object with attributionRequired and shareAlike, driver mandate/incentive)
    - REQUIREMENTS.md DATA-01, DATA-02, TRUST-06
  </read_first>
  <action>
    Create schemas/source.schema.json (JSON Schema draft 2020-12, $id under https://foodtransparency.uk/schemas/source.schema.json) describing one registry record with required id, name, publisher, url, covers, updateFrequency, retrievedDate, sourceType, jurisdiction, licence; and optional driver. sourceType is an enum of primary, secondary, tertiary, grey. jurisdiction is an enum of GB, EU, international. licence is an object with id, url, attributionRequired (boolean), shareAlike (boolean). driver is either null or an object with type (enum mandate, incentive) and name. Set additionalProperties false. Drop DEBT's per-source confidence_level entirely (confidence is now a per-fact axis).
    Create src/_data/sources.json as an object with a sources array seeded with at least: the Open Food Facts record (id "off", sourceType tertiary, jurisdiction international, licence ODbL-1.0 with attributionRequired true and shareAlike true, driver null) exactly per the research example; one GB regulatory source (e.g. an FSA record with jurisdiction "GB"); and one policy-driver source carrying driver.type "incentive" (e.g. the 2018 Soft Drinks Industry Levy) to exercise the mandate-vs-incentive flag. Every record must validate against source.schema.json. British English throughout.
  </action>
  <verify>
    <automated>node -e "const s=require('./src/_data/sources.json'); const off=s.sources.find(x=>x.id==='off'); if(!off||off.licence.shareAlike!==true||off.jurisdiction!=='international') process.exit(1); if(!s.sources.some(x=>x.jurisdiction==='GB')) process.exit(1); if(!s.sources.some(x=>x.driver&&x.driver.type)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>source.schema.json defines the extended record with sourceType, jurisdiction, and a licence object; sources.json contains the OFF ODbL record (shareAlike true), at least one GB-jurisdiction source, and at least one driver-tagged policy source. No record carries a per-source confidence_level.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Open-data licence declaration and README licence split</name>
  <files>LICENSE-DATA, README.md</files>
  <read_first>
    - 01-RESEARCH.md section "## Open-Data Licence Decision (DATA-12)" (ODbL 1.0 primary recommendation; CC BY 4.0 flagged alternative; in-repo declaration requirements)
    - REQUIREMENTS.md DATA-12
  </read_first>
  <action>
    Create LICENSE-DATA containing the full ODbL 1.0 licence text (Open Database License 1.0) as the dataset licence; this is the research-recommended default. Add a short header comment in LICENSE-DATA noting that CC BY 4.0 is the flagged alternative considered, and that the canonical choice is recorded in meta.json datasetLicence. Update README.md with a "Licensing" section stating the split: code is MIT (see LICENSE), the canonical git-versioned dataset is ODbL 1.0 (see LICENSE-DATA), every source record carries its own licence object, and any Open Food Facts derived fact is identifiable by its "off" source id (shareAlike satisfied in place). British English, no em-dashes (README is scanned by the editorial gate).
  </action>
  <verify>
    <automated>test -f LICENSE-DATA && grep -qi "ODbL\|Open Database License" LICENSE-DATA && grep -qi "Licensing\|licence" README.md</automated>
  </verify>
  <acceptance_criteria>LICENSE-DATA holds the ODbL 1.0 text and notes the CC BY 4.0 alternative; README documents the MIT-code / ODbL-data split and the OFF-derived identifiability; meta.json datasetLicence already records the canonical choice (Task 1).</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editorial authoring to data store | Human-authored registry records enter the canonical JSON; integrity enforced at build by the referential gate (plan 01-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Registry record drift (a fact cites an unknown or wrong-jurisdiction source) | mitigate | source.schema.json shape gate plus the referential gate (plan 01-05) that resolves every cited id and enforces GB jurisdiction for regulatory facts (TRUST-06) |
| T-02-02 | Repudiation | Licence obligation not auditable | mitigate | Per-source licence object with attributionRequired and shareAlike; datasetLicence in meta.json; LICENSE-DATA in repo |
| T-02-03 | Information disclosure | None (all data public) | accept | Public archive by design; no secrets in registry |
</threat_model>

<verification>
- meta.json validates with two GRADE enums and a datasetLicence.
- Every sources.json record validates against source.schema.json (proved fully by the schema gate in plan 01-05).
- LICENSE-DATA and the README licence section exist.
</verification>

<success_criteria>
The two-axis trust vocabulary, the auditable source/rights registry with jurisdiction and driver flags, and the open-data licence declaration are all in place and self-consistent, with source-type and licence on the registry and confidence/evidence as separate per-fact axes.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-02-SUMMARY.md` when done.
</output>
