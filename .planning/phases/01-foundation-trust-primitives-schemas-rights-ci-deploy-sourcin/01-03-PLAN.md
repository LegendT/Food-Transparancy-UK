---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - schemas/sourced-value.schema.json
  - schemas/date-value.schema.json
  - src/_data/allergens.json
autonomous: true
requirements: [TRUST-01, TRUST-02, DATA-03, DATA-07, DATA-09, DATA-11]
user_setup: []

must_haves:
  truths:
    - "Every fact-bearing field is one shared SourcedValue envelope: value, sources, confidence, evidence, updated, claimType, with optional checkedOn and claimDomain"
    - "A corroborable fact structurally requires at least two DISTINCT sources; an authoritative fact requires at least one"
    - "Dates support ranged/circa values without forcing false precision; a range from/to accepts a full ISO date or a 4-digit year"
    - "The 14 GB-regulated allergens are a controlled vocabulary, not free text"
    - "Verification-status and publication-status fields are reserved (present-and-empty allowed) for Phase 2"
  artifacts:
    - path: "schemas/sourced-value.schema.json"
      provides: "The reusable provenance envelope $ref'd by every fact-bearing field; the DATA-09 designation in practice; optional checkedOn and the claimDomain discriminator"
      contains: "claimType"
    - path: "schemas/date-value.schema.json"
      provides: "Precise-or-ranged date contract (DATA-03); from/to as date-or-year"
      contains: "oneOf"
    - path: "src/_data/allergens.json"
      provides: "The 14 GB-regulated major allergens controlled vocabulary (DATA-07)"
  key_links:
    - from: "schemas/sourced-value.schema.json"
      to: "meta.json grade enums"
      via: "confidence and evidence use the GRADE 4-point $defs grade enum (kept in sync with meta.json, verified by the consistency test in plan 01-04)"
      pattern: "grade"
---

<objective>
Define the load-bearing trust primitives: the SourcedValue envelope every fact-bearing field reuses by $ref (TRUST-01, DATA-09), the structural corroborable-vs-authoritative claim-type rule (DATA-11), the optional checkedOn date and claimDomain discriminator that the TRUST-06 gate keys on, ranged/circa dates (DATA-03), and the 14-allergen controlled vocabulary (DATA-07). The envelope reserves empty verification-status and publication-status fields for the Phase 2 gate to populate.

Purpose: This is the contract every entity schema (plan 01-04) and the validation gate (plan 01-05) build against. Defining it once means the trust contract cannot drift per field.
Output: schemas/sourced-value.schema.json, schemas/date-value.schema.json, src/_data/allergens.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The SourcedValue envelope and the structural claim-type rule</name>
  <files>schemas/sourced-value.schema.json</files>
  <read_first>
    - 01-RESEARCH.md section "### Pattern 1: The `SourcedValue` envelope as a reusable `$ref`" (the full schema including required fields, the grade $def, the optional checkedOn and claimDomain, uniqueItems on sources, the reserved verificationStatus/publicationStatus, and the allOf if/then claim-type rule)
    - 01-RESEARCH.md "## Common Pitfalls" Pitfall 5 (Phase 1 enforces the STRUCTURAL claim-type shape only; pass-counting is Phase 2)
    - REQUIREMENTS.md TRUST-01, TRUST-06, DATA-09, DATA-11
  </read_first>
  <action>
    Create schemas/sourced-value.schema.json (draft 2020-12; declare BOTH `"$schema": "https://json-schema.org/draft/2020-12/schema"` and `$id` https://foodtransparency.uk/schemas/sourced-value.schema.json) per the research Pattern 1 block. Required: value (any type), sources (array of source-id strings, minItems 1, uniqueItems true so a corroborable fact citing ["off","off"] cannot satisfy minItems 2 — lineage-level distinctness remains a Phase 2 concern), confidence ($ref to the grade $def), evidence ($ref to the grade $def), updated (string, format date), claimType (enum corroborable, authoritative). Optional: checkedOn (string, format date) — REQUIRED by the referential gate for regulatory facts (TRUST-06), rendered by the macro (plan 01-07), and consumed by the referential check (plan 01-05); claimDomain (enum general, regulatory, nutrition, allergen, ingredient-function) — the discriminator the TRUST-06 gate keys on, where claimDomain "regulatory" triggers the GB-source + checkedOn rule; note (string); and the reserved Phase 2 fields verificationStatus and publicationStatus, each typed ["string","null"] so a present-and-empty value validates. Define $defs.grade as enum high, moderate, low, very-low. Set additionalProperties false. Add the allOf if/then structural rule: if claimType is corroborable then sources.minItems is 2; an authoritative fact keeps the base minItems 1. Restate `"type":"object"` on the `if` branch and `"type":"array"` on the `then`'s `sources` subschema so Ajv strictTypes:"log" emits no warning and the rule is robust under strictTypes:true (the transcription re-read record is added by Phase 2, do not model pass-counting here). Do NOT put source-type on the envelope (it lives on the source record). Do NOT add a combined trust score.
    DATA-09 boundary note (carry into the SUMMARY): "fact-bearing iff the schema uses the SourcedValue $ref" enforces provenance only on fields ALREADY declared as the $ref. It cannot catch an empirical field mistakenly modelled as a bare scalar; that omission is caught by the per-entity DATA-09 guard test in plan 01-04, not by the envelope.
  </action>
  <verify>
    <automated>node -e "const s=require('./schemas/sourced-value.schema.json'); const p=s.properties||{}; const r=s.required||[]; if(s.\$schema!=='https://json-schema.org/draft/2020-12/schema')process.exit(1); ['value','sources','confidence','evidence','updated','claimType'].forEach(k=>{if(!r.includes(k))process.exit(1)}); if(s.additionalProperties!==false)process.exit(1); if(!p.checkedOn||p.checkedOn.format!=='date')process.exit(1); if(!p.claimDomain||!(p.claimDomain.enum||[]).includes('regulatory'))process.exit(1); if(p.sources.uniqueItems!==true)process.exit(1); if(!JSON.stringify(s).includes('verificationStatus')||!JSON.stringify(s).includes('corroborable'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>sourced-value.schema.json requires value/sources/confidence/evidence/updated/claimType, sets additionalProperties false, sets sources uniqueItems true, carries optional checkedOn (format date) and claimDomain (enum incl. regulatory), reserves verificationStatus and publicationStatus as nullable optional, and encodes the corroborable-requires-2-sources structural rule. The Ajv gate in plan 01-05 will prove the failure paths.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Ranged/circa date contract</name>
  <files>schemas/date-value.schema.json</files>
  <read_first>
    - 01-RESEARCH.md section "### Pattern 1" Ranged date model note (from/to each as anyOf full-ISO-date or 4-digit year; to < from is NOT expressible in JSON Schema, so the order check is imperative in plan 01-05)
    - 01-RESEARCH.md "## Architecture Patterns" Anti-Patterns note on ranged dates with a basis (DATA-03; "between 2012 and 2014", "circa 2015")
    - REQUIREMENTS.md DATA-03
  </read_first>
  <action>
    Create schemas/date-value.schema.json (draft 2020-12; declare BOTH `"$schema": "https://json-schema.org/draft/2020-12/schema"` and `$id` https://foodtransparency.uk/schemas/date-value.schema.json) as a oneOf: either a precise date string (format date), OR a tagged range object with required from and to and an optional basis string, OR a circa object with required circa (a year or date) and optional basis. Specify range from and to EACH as {"anyOf":[{"type":"string","format":"date"},{"type":"string","pattern":"^[0-9]{4}$"}]} so a full ISO date or a 4-digit year validates and an arbitrary string does not. JSON Schema CANNOT express "to not earlier than from": document that constraint in the schema description and leave the imperative order check (checkDateRanges) to the validation gate in plan 01-05 — do NOT attempt it in Ajv here. additionalProperties false on the object branches. This schema is referenced by timeline-event and any dated fact.
  </action>
  <verify>
    <automated>node -e "const s=require('./schemas/date-value.schema.json'); const j=JSON.stringify(s); if(s.\$schema!=='https://json-schema.org/draft/2020-12/schema')process.exit(1); if(!Array.isArray(s.oneOf)||s.oneOf.length<2)process.exit(1); if(!j.includes('basis'))process.exit(1); if(!j.includes('anyOf')||!j.includes('^[0-9]{4}$'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>date-value.schema.json is a oneOf admitting a precise date, a from/to range (each date-or-year via anyOf) with optional basis, and a circa form. A structurally malformed range (missing from, non-date/non-year) is rejectable by Ajv; the to-before-from ORDER fixture is asserted against the imperative checkDateRanges in plan 01-05, not Ajv.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: The 14-allergen controlled vocabulary</name>
  <files>src/_data/allergens.json</files>
  <read_first>
    - 01-RESEARCH.md section "## Allergen Field (DATA-07)" (the 14 GB-regulated allergens list; the allergen-item shape with allergen id, presence enum present/may-contain/absent, and the SourcedValue fields; the note to verify wording against current FSA guidance)
    - REQUIREMENTS.md DATA-07
  </read_first>
  <action>
    Create src/_data/allergens.json as a controlled vocabulary listing the 14 GB-regulated major allergens with stable ids and display names: celery, cereals-containing-gluten, crustaceans, eggs, fish, lupin, milk, molluscs, mustard, tree-nuts, peanuts, sesame, soybeans, sulphur-dioxide-and-sulphites. Include a top-level note field recording that the wording must be verified against current FSA allergen guidance before publish (Assumption A2 in research). This file is the enum source the product schema (plan 01-04) is kept in sync with (verified by the consistency test in plan 01-04); a 15th or mistyped allergen must be rejectable by the product schema enum. British English (sulphur, not sulfur).
  </action>
  <verify>
    <automated>node -e "const a=require('./src/_data/allergens.json'); const ids=(a.allergens||a.items||[]).map(x=>x.id||x); const need=['celery','milk','peanuts','sesame','sulphur-dioxide-and-sulphites']; need.forEach(n=>{if(!ids.includes(n))process.exit(1)}); if(ids.length!==14)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>allergens.json lists exactly the 14 GB allergens with British-English ids and a verify-against-FSA note. The product schema enum (plan 01-04) is kept in sync with this set (verified by test) so a typo or 15th allergen fails validation.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema definition to all data | These schemas are the integrity boundary for every fact-bearing field; a weak schema lets bad data through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | A fact-bearing field published without full provenance | mitigate | SourcedValue requires source, confidence, evidence, updated; additionalProperties false; enforced by the Ajv gate (plan 01-05) and proved by a missing-provenance negative fixture |
| T-03-02 | Tampering | False precision on uncertain dates | mitigate | date-value supports ranged/circa with a basis (date-or-year) so uncertainty is recorded, not invented |
| T-03-03 | Tampering | Allergen misclassification (may-contain silently equated with present) | mitigate | Three-state presence vocabulary and a closed 14-id enum |
| T-03-04 | Tampering | A corroborable fact faking two sources by repeating one id | mitigate | sources uniqueItems true so ["off","off"] cannot satisfy minItems 2 |
</threat_model>

<verification>
- All three schemas are valid JSON and parse.
- The structural claim-type rule, the optional checkedOn/claimDomain, and the reserved Phase 2 fields are present (proved end-to-end by the gate in plan 01-05).
</verification>

<success_criteria>
The reusable SourcedValue envelope (with checkedOn, claimDomain and uniqueItems sources), the ranged-date contract, and the 14-allergen vocabulary exist and define the trust contract once, so entity schemas and the gate can build against them without drift.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-03-SUMMARY.md` when done.
</output>
