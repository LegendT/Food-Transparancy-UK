---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 05
type: execute
wave: 4
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - lib/validate.mjs
  - lib/referential.mjs
  - scripts/validate-data.mjs
  - test/schema.test.js
  - test/referential.test.js
  - test/fixtures/valid/product-valid.json
  - test/fixtures/valid/timeline-valid.json
  - test/fixtures/invalid/missing-provenance.json
  - test/fixtures/invalid/corroborable-one-source.json
  - test/fixtures/invalid/bad-ranged-date.json
  - test/fixtures/invalid/inference-as-sourcedvalue.json
  - test/fixtures/invalid/bad-allergen.json
  - test/fixtures/invalid/unknown-source-id.json
  - test/fixtures/invalid/regulatory-non-gb.json
autonomous: true
requirements: [TRUST-01, TRUST-05, TRUST-06, DATA-01, DATA-03, DATA-04, DATA-07, DATA-09, DATA-11]
user_setup: []

must_haves:
  truths:
    - "The build fails if any fact-bearing field is missing a source, confidence, evidence or update date (TRUST-05)"
    - "A corroborable fact with one source fails; a malformed ranged date fails; a 15th allergen fails; an inference shaped as a SourcedValue fails"
    - "A fact citing an unknown source id fails; a regulatory fact citing a non-GB source fails (TRUST-06)"
    - "Each gate is proved by a negative fixture, not just a happy path"
  artifacts:
    - path: "lib/validate.mjs"
      provides: "Ajv 2020 compile-and-validate over the schemas dir; returns structured errors"
      contains: "Ajv2020"
    - path: "lib/referential.mjs"
      provides: "Source-id resolution, TRUST-06 GB-jurisdiction check, OFF-derived tagging"
      contains: "jurisdiction"
    - path: "scripts/validate-data.mjs"
      provides: "CLI wrapper run by prebuild; process.exit(1) on any error"
      exports: ["(cli)"]
    - path: "test/schema.test.js"
      provides: "Happy-path validation plus every structural negative fixture"
  key_links:
    - from: "scripts/validate-data.mjs"
      to: "package.json prebuild"
      via: "npm run validate is the first link in the prebuild chain"
      pattern: "validate-data"
---

<objective>
Build the data validation gate: Ajv structural validation (TRUST-05, DATA-11, DATA-03, DATA-04, DATA-07, claim-type structural) plus cross-file referential integrity (DATA-01 source-id resolution, TRUST-06 GB-jurisdiction for regulatory facts, OFF-derived tagging). Both run inside scripts/validate-data.mjs, wired into prebuild by plan 01-01. Every failure path ships with a negative fixture that proves the build fails on bad input. A gate is only real if its failure path is tested.

Purpose: This makes the core value a compile error, not a hope.
Output: lib/validate.mjs, lib/referential.mjs, scripts/validate-data.mjs, two test files, the valid + invalid fixture corpus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-VALIDATION.md
@./CLAUDE.md

<interfaces>
<!-- Built in plans 01-02/03/04, consumed here directly: -->
- schemas/sourced-value.schema.json, schemas/date-value.schema.json, schemas/source.schema.json
- schemas/{product,ingredient,brand,additive,image,timeline-event}.schema.json
- src/_data/sources.json (registry; ids resolved by referential.mjs), src/_data/allergens.json (14 ids)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ajv validator and referential gate logic in lib/</name>
  <files>lib/validate.mjs, lib/referential.mjs, scripts/validate-data.mjs</files>
  <behavior>
    - lib/validate.mjs compiles every schemas/*.schema.json under Ajv 2020 with ajv-formats and validates a given dataset object, returning {errors:[...]} with readable instancePath messages.
    - lib/referential.mjs returns an error for any fact whose sources[] contains an id absent from sources.json; returns an error for any field marked as a regulatory position that cites no jurisdiction:"GB" source or lacks a checkedOn date (TRUST-06); exposes a helper that lists OFF-derived facts (any fact citing the "off" source id).
    - scripts/validate-data.mjs runs both over the real src/_data entity files and process.exit(1) with the error list on the first failure, exit 0 when clean.
  </behavior>
  <read_first>
    - 01-RESEARCH.md section "### Ajv runner (`lib/validate.mjs`, draft 2020-12)" (the exact import of ajv/dist/2020.js, addFormats, addSchema loop, strict:true allErrors:true)
    - 01-RESEARCH.md section "### Pattern 4: Gates share `lib/` logic with their tests" (lib pure functions returning {errors}; scripts wrap and exit)
    - 01-RESEARCH.md section "## Source / Rights Registry" (the TRUST-06 jurisdiction rule and OFF-derived-by-construction tagging)
    - /Users/anthonygeorge/Projects/DEBT/test/data.test.js (the node:test load-and-assert pattern to mirror)
  </read_first>
  <action>
    Create lib/validate.mjs exporting a compile(schemaDir) that builds an Ajv2020 instance (allErrors true, strict true), calls addFormats for date and uri, and addSchema for every *.schema.json in schemas/; and a validateDataset(ajv, files) returning {errors}. Mirror the research Ajv runner block exactly. Create lib/referential.mjs exporting checkReferences(facts, sources) and checkRegulatoryJurisdiction(facts, sources) returning {errors}, plus listOffDerived(facts, sources). A regulatory-position field cites at least one jurisdiction GB source and carries a checkedOn date or it errors. Create scripts/validate-data.mjs that imports both libs, loads the entity files from src/_data, runs validate then referential, prints a readable error list and process.exit(1) on any error. Keep error handling at this boundary only (do not wrap internal calls in speculative try/catch). British English in all messages, no em-dashes.
  </action>
  <verify>
    <automated>node scripts/validate-data.mjs && echo "clean dataset passes"</automated>
  </verify>
  <acceptance_criteria>With the seed/valid data present, `node scripts/validate-data.mjs` exits 0. lib/validate.mjs uses Ajv2020 + ajv-formats and loads all schemas; lib/referential.mjs enforces source-id resolution and the TRUST-06 GB-jurisdiction rule and can list OFF-derived facts.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Happy-path tests and valid fixtures</name>
  <files>test/schema.test.js, test/referential.test.js, test/fixtures/valid/product-valid.json, test/fixtures/valid/timeline-valid.json</files>
  <behavior>
    - test/schema.test.js: a valid product fixture and a valid timeline fixture both pass Ajv with zero errors.
    - test/referential.test.js: a fixture whose facts cite only known source ids and whose regulatory field cites a GB source passes with zero errors.
  </behavior>
  <read_first>
    - /Users/anthonygeorge/Projects/DEBT/test/data.test.js and test/no-emdash.test.js (node:test import style and assertion patterns)
    - schemas/* (the shapes the fixtures must satisfy)
    - 01-VALIDATION.md "### Phase Requirements -> Test Map" (the rows this plan must cover)
  </read_first>
  <action>
    Create test/fixtures/valid/product-valid.json: a fully provenance-complete product with at least one fact-bearing field as a SourcedValue (value, sources resolving to a real registry id, confidence and evidence from the GRADE enum, updated date, claimType), a structured allergen item, and a regulatory-position field citing a GB source with a checkedOn date. Create test/fixtures/valid/timeline-valid.json: a timeline event with a ranged date and the three separate change fields, labelledInference as a non-SourcedValue with a basis. Create test/schema.test.js using node:test that compiles the schemas via lib/validate.mjs and asserts both valid fixtures validate clean. Create test/referential.test.js asserting the valid fixture passes checkReferences and checkRegulatoryJurisdiction.
  </action>
  <verify>
    <automated>node --test test/schema.test.js test/referential.test.js</automated>
  </verify>
  <acceptance_criteria>Both test files pass: the valid product and timeline fixtures validate under Ajv with zero errors and clear referential checks.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Negative fixtures proving every gate fails on bad input</name>
  <files>test/schema.test.js, test/referential.test.js, test/fixtures/invalid/missing-provenance.json, test/fixtures/invalid/corroborable-one-source.json, test/fixtures/invalid/bad-ranged-date.json, test/fixtures/invalid/inference-as-sourcedvalue.json, test/fixtures/invalid/bad-allergen.json, test/fixtures/invalid/unknown-source-id.json, test/fixtures/invalid/regulatory-non-gb.json</files>
  <behavior>
    - Each invalid fixture is rejected by the gate that owns its failure mode; the test asserts a non-empty error set for each.
    - scripts/validate-data.mjs exits non-zero when pointed at any invalid fixture.
  </behavior>
  <read_first>
    - 01-VALIDATION.md "### Phase Requirements -> Test Map" and "## Wave 0 Requirements" (the one-failing-fixture-per-gate mandate)
    - 01-RESEARCH.md "## Common Pitfalls" Pitfall 2 (the gates must fail the build, not just the test) and Pitfall 5 (structural claim-type only)
    - schemas/sourced-value.schema.json, schemas/date-value.schema.json, schemas/product.schema.json, schemas/timeline-event.schema.json
  </read_first>
  <action>
    Create the seven invalid fixtures, each a minimal entity that violates exactly one rule: missing-provenance.json (a fact-bearing field with no sources / no confidence / no evidence / no updated, TRUST-05); corroborable-one-source.json (claimType corroborable with sources length 1, the structural claim-type rule); bad-ranged-date.json (a range with to earlier than from or a missing from, DATA-03); inference-as-sourcedvalue.json (a timeline labelledInference carrying value/confidence/evidence like a SourcedValue, DATA-04 forbidden); bad-allergen.json (an allergen id not in the 14, or a 15th, DATA-07); unknown-source-id.json (a fact citing a source id absent from sources.json, DATA-01); regulatory-non-gb.json (a regulatory-position field citing only an EU/EFSA source with no GB source, TRUST-06). Extend test/schema.test.js to assert the schema-level fixtures (missing-provenance, corroborable-one-source, bad-ranged-date, inference-as-sourcedvalue, bad-allergen) each produce Ajv errors. Extend test/referential.test.js to assert unknown-source-id and regulatory-non-gb each produce referential errors. Add a test asserting scripts/validate-data.mjs exits non-zero when run against an invalid fixture (spawn it as a child process and check the exit code).
  </action>
  <verify>
    <automated>node --test test/schema.test.js test/referential.test.js</automated>
  </verify>
  <acceptance_criteria>All seven negative fixtures are rejected by their owning gate and the tests pass by asserting the rejections. The child-process check confirms `node scripts/validate-data.mjs` exits non-zero on bad input, proving the prebuild gate fails the build (TRUST-05 and the claim-type, date, inference, allergen, source-id and jurisdiction rules).</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored/ingested data to published build | The validation gate is the integrity boundary; nothing renders unless it passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | A fact published without complete provenance | mitigate | Ajv required-fields gate in prebuild; missing-provenance negative fixture proves the build fails (TRUST-05) |
| T-05-02 | Tampering | Inference laundered into fact | mitigate | inference-as-sourcedvalue fixture proves the DATA-04 separation rejects it |
| T-05-03 | Spoofing | A fact citing a non-existent or wrong-jurisdiction source | mitigate | referential gate resolves every source id and enforces GB jurisdiction for regulatory facts (TRUST-06); two negative fixtures |
| T-05-04 | Tampering | Gate exists as test only, so a bad commit still deploys | mitigate | Logic lives in scripts/validate-data.mjs wired into prebuild (plan 01-01); child-process exit-code test proves the build path fails |
</threat_model>

<verification>
- `npm test` passes (happy path plus all negative assertions).
- `node scripts/validate-data.mjs` exits 0 on clean data and non-zero on any invalid fixture.
</verification>

<success_criteria>
The structural and referential validation gates are wired into the build, every failure mode (provenance, claim-type, ranged date, inference, allergen, source-id, jurisdiction) is proved by a negative fixture, and a bad fixture makes the build fail.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-05-SUMMARY.md` when done.
</output>
