---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 06
type: execute
wave: 4
depends_on: [01-01, 01-04]
files_modified:
  - lib/check-images.mjs
  - scripts/check-images.mjs
  - src/_data/images.json
  - lib/editorial-rules.mjs
  - scripts/check-editorial.mjs
  - test/images.test.js
  - test/editorial.test.js
  - test/fixtures/invalid/uncleared-packshot.json
  - test/fixtures/invalid/unmanifested-image.json
  - test/fixtures/invalid/editorial-emdash.md
  - test/fixtures/invalid/editorial-us-spelling.md
  - test/fixtures/invalid/editorial-superlative.md
  - test/fixtures/invalid/editorial-motive.md
autonomous: true
requirements: [DATA-10, UX-06]
user_setup: []

must_haves:
  truths:
    - "The build blocks any referenced brand image whose rights status is not own-photographed, cleared, or recorded fair-dealing; default is not-cleared"
    - "A referenced-but-unmanifested image is treated as not-cleared and fails"
    - "The build fails on an em-dash, a non-en-GB spelling, a superlative/denigratory term, or a causal-motive verb in a narrative"
    - "Each gate is proved by a negative fixture"
  artifacts:
    - path: "lib/check-images.mjs"
      provides: "DATA-10 default-deny logic over referenced images vs the manifest"
      contains: "not-cleared"
    - path: "lib/editorial-rules.mjs"
      provides: "Curated en-GB and neutral-editorial denylists (US spellings, superlatives, motive phrases, em-dash)"
      contains: "MOTIVE_PHRASES"
    - path: "scripts/check-editorial.mjs"
      provides: "CLI lint run by prebuild; word-boundary matching over prose only"
  key_links:
    - from: "scripts/check-images.mjs"
      to: "package.json prebuild"
      via: "npm run check:images is the third link in the prebuild chain"
      pattern: "check-images"
    - from: "scripts/check-editorial.mjs"
      to: "package.json prebuild"
      via: "npm run lint:editorial is the second link in the prebuild chain"
      pattern: "check-editorial"
---

<objective>
Build the two text/asset gates wired into prebuild by plan 01-01: the image-rights default-deny gate (DATA-10) and the British-English plus neutral-editorial lint (UX-06). Each ships with negative fixtures proving the build fails on an uncleared image, an em-dash, a US spelling, a superlative, and a causal-motive verb. The Phase 4 data-level no-imputation-of-motive publication gate is NOT built here; Phase 1 ships the text lint only.

Purpose: Default-deny on imagery and a neutral British editorial voice become structural invariants before any content exists.
Output: lib/check-images.mjs, scripts/check-images.mjs, images.json, lib/editorial-rules.mjs, scripts/check-editorial.mjs, two test files, the negative fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-VALIDATION.md
@./CLAUDE.md

<interfaces>
- schemas/image.schema.json (from plan 01-04; rightsStatus enum own-photographed/cleared/fair-dealing-criticism/not-cleared)
- /Users/anthonygeorge/Projects/DEBT/test/no-emdash.test.js (the em-dash scan to extend: literal U+2014 and %E2%80%94 encoded; en-dash U+2013 allowed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Image-rights default-deny gate</name>
  <files>lib/check-images.mjs, scripts/check-images.mjs, src/_data/images.json</files>
  <behavior>
    - Any image referenced by entity data whose manifest rightsStatus is not-cleared (or which has no manifest record) errors.
    - An image with rightsStatus own-photographed, cleared, or fair-dealing-criticism (with a justification) passes.
    - Brand names used as identifiers are not images and are never gated.
  </behavior>
  <read_first>
    - 01-RESEARCH.md "## Common Pitfalls" Pitfall 4 (gate what is referenced, not the manifest in isolation; a referenced-but-unmanifested image is not-cleared)
    - 01-RESEARCH.md "### Pattern 4: Gates share `lib/` logic with their tests"
    - schemas/image.schema.json (the rights record contract)
    - REQUIREMENTS.md DATA-10
  </read_first>
  <action>
    Create src/_data/images.json as the rights manifest: an images array, each item validating against image.schema.json, seeded with at least one allowed example (own-photographed) so the happy path is non-trivial. Create lib/check-images.mjs exporting checkImages(referencedImages, manifest) returning {errors}: it enumerates images referenced by entity data, looks each up in the manifest by reference, and errors when the record is missing or its rightsStatus is not-cleared (default-deny). Treat fair-dealing-criticism as allowed only when a justification string is present. Create scripts/check-images.mjs that gathers referenced images from src/_data entity files and the manifest, runs checkImages, prints offenders, and process.exit(1) on any error. British English in messages.
  </action>
  <verify>
    <automated>node scripts/check-images.mjs && echo "image gate passes on clean data"</automated>
  </verify>
  <acceptance_criteria>With a clean manifest, `node scripts/check-images.mjs` exits 0. checkImages errors on a not-cleared or unmanifested referenced image and passes own-photographed/cleared/justified-fair-dealing. Brand identifier strings are not treated as images.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: British-English and neutral-editorial lint</name>
  <files>lib/editorial-rules.mjs, scripts/check-editorial.mjs</files>
  <behavior>
    - The lint flags a literal or URL-encoded em-dash, a US spelling, a superlative/denigratory term, and a causal-motive phrase, each by word-boundary match over prose.
    - It does NOT flag a denylist token appearing inside a legitimate word or proper noun (word-boundary, not substring).
  </behavior>
  <read_first>
    - /Users/anthonygeorge/Projects/DEBT/test/no-emdash.test.js (the file-walk, TEXT_EXT set, literal + encoded em-dash detection to extend)
    - 01-RESEARCH.md "### Editorial lint denylists (`lib/editorial-rules.mjs`)" (the exact US_SPELLINGS, SUPERLATIVES, MOTIVE_PHRASES, EM_DASH constants)
    - 01-RESEARCH.md "## Common Pitfalls" Pitfall 3 (word-boundary matching; scan prose only; report offender + file + line)
    - 01-RESEARCH.md "## Open Questions" item 2 (sentence-case is a best-effort warning in Phase 1, not a hard gate)
    - REQUIREMENTS.md UX-06
  </read_first>
  <action>
    Create lib/editorial-rules.mjs exporting the curated denylists per research: US_SPELLINGS (color, flavor, fiber, center, organize, organization, analyze, behavior, defense, labeled, modeling, favorite, license-the-verb), SUPERLATIVES (worst, scandal, shocking, outrage, disgraceful, slammed, exposed), MOTIVE_PHRASES (regexes: to boost margins, to cut costs, to increase profits, to save money, greed, cynical), and the em-dash constants (literal U+2014 plus the %E2%80%94 encoded form). Export a lint(text) that returns offenders with rule, match and position, using word-boundary regexes (\\b) so substrings inside legitimate words or brand names are not flagged. Create scripts/check-editorial.mjs extending DEBT's no-emdash file walk: scan src and docs prose (.njk, .md text) and designated narrative fields, collect offenders with file + line, and process.exit(1) when any are found. Make sentence-case a best-effort warning only (printed, non-failing) in Phase 1. British English throughout; this file must itself pass the lint.
  </action>
  <verify>
    <automated>node scripts/check-editorial.mjs && echo "editorial lint passes on the clean repo"</automated>
  </verify>
  <acceptance_criteria>`node scripts/check-editorial.mjs` exits 0 on the clean repo. lib/editorial-rules.mjs exports the four denylist categories and a word-boundary lint() that does not false-positive on substrings. Sentence-case is a non-failing warning.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Negative fixtures proving both gates fail the build</name>
  <files>test/images.test.js, test/editorial.test.js, test/fixtures/invalid/uncleared-packshot.json, test/fixtures/invalid/unmanifested-image.json, test/fixtures/invalid/editorial-emdash.md, test/fixtures/invalid/editorial-us-spelling.md, test/fixtures/invalid/editorial-superlative.md, test/fixtures/invalid/editorial-motive.md</files>
  <behavior>
    - uncleared-packshot and unmanifested-image fixtures each make checkImages error.
    - Each editorial fixture (em-dash, US spelling, superlative, motive phrase) makes lint() return at least one offender.
    - The fixtures live under test/fixtures so the repo-wide editorial scan does not flag them in normal runs (the tests pass the fixture text to lint() directly).
    - Each gate SCRIPT, spawned as a child process against a bad fixture, exits non-zero — proving the gate fails the build (prebuild), not only the unit test (research Pitfall 2; mirrors Plan 01-05 Task 3).
  </behavior>
  <read_first>
    - 01-VALIDATION.md "### Phase Requirements -> Test Map" rows for DATA-10 and UX-06, and "## Wave 0 Requirements"
    - /Users/anthonygeorge/Projects/DEBT/test/no-emdash.test.js (assertion style)
    - lib/check-images.mjs and lib/editorial-rules.mjs (from Tasks 1 and 2)
  </read_first>
  <action>
    Create the image negative fixtures: uncleared-packshot.json (a manifest image with rightsStatus not-cleared that is referenced) and unmanifested-image.json (entity data referencing an image with no manifest record). Create the four editorial fixtures as small Markdown snippets each containing exactly one violation: an em-dash; a US spelling (e.g. "color"); a superlative ("shocking"); a causal-motive phrase ("to cut costs"). Create test/images.test.js asserting checkImages returns errors for both image fixtures and zero errors for an allowed image. Create test/editorial.test.js reading each editorial fixture and asserting lint() returns at least one offender of the expected rule, and asserting clean British text returns none. Ensure scripts/check-editorial.mjs excludes test/fixtures from its repo scan (mirror DEBT's SKIP set approach) so the deliberately-bad fixtures do not break the real lint run. In BOTH test files, add a child-process assertion (use node:child_process spawnSync, mirroring Plan 01-05 Task 3's pattern for validate-data.mjs): spawn scripts/check-images.mjs pointed at the uncleared-packshot fixture and assert status is non-zero; spawn scripts/check-editorial.mjs pointed at the editorial-emdash fixture and assert status is non-zero — proving each gate fails the build, not only the unit test.
  </action>
  <verify>
    <automated>node --test test/images.test.js test/editorial.test.js && node scripts/check-editorial.mjs</automated>
  </verify>
  <acceptance_criteria>Both test files pass by asserting the gates reject the negative fixtures; the editorial fixtures are excluded from the repo-wide scan so `node scripts/check-editorial.mjs` still exits 0. The image gate rejects both a not-cleared and an unmanifested referenced image (DATA-10); the editorial gate rejects an em-dash, a US spelling, a superlative, and a motive phrase (UX-06). A child-process spawn of scripts/check-images.mjs against a not-cleared fixture exits non-zero, and a child-process spawn of scripts/check-editorial.mjs against an em-dash fixture exits non-zero (build-fail proof, mirroring Plan 01-05 Task 3).</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored prose/imagery to published build | The editorial and image gates are the legal-and-voice integrity boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering / Repudiation | Uncleared brand imagery reaches a published page (IP exposure) | mitigate | Default-deny image gate over referenced images; not-cleared and unmanifested fixtures prove the build fails (DATA-10) |
| T-06-02 | Tampering | Denigratory or motive-imputing copy ships (defamation exposure) | mitigate | Editorial lint denylists with word-boundary matching; superlative and motive fixtures prove the build fails (UX-06). Phase 1 is text-only; the data-level motive gate is Phase 4 |
| T-06-03 | Tampering | Lint false-positive blocks legitimate copy | accept (low) | Word-boundary regexes and prose-only scanning per Pitfall 3; denylists kept small and curated |
</threat_model>

<verification>
- `npm test` passes (image and editorial happy paths plus all negative assertions).
- `node scripts/check-images.mjs` and `node scripts/check-editorial.mjs` exit 0 on the clean repo and the gate logic rejects each negative fixture.
</verification>

<success_criteria>
The image-rights default-deny gate and the British-English/neutral-editorial lint are wired into prebuild, each failure mode is proved by a negative fixture, and the clean repo still builds.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-06-SUMMARY.md` when done.
</output>
