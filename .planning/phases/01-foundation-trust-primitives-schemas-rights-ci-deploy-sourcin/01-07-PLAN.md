---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 07
type: execute
wave: 5
depends_on: [01-02, 01-03, 01-05, 01-06]
files_modified:
  - src/_includes/components/macros.njk
  - src/_data/demoFact.json
  - src/components-demo.njk
  - src/methodology.njk
  - src/index.njk
  - src/404.njk
  - src/assets/styles.css
autonomous: true
requirements: [TRUST-03, TRUST-04]
user_setup: []

must_haves:
  truths:
    - "A reader can see the source(s) behind a displayed claim via an inline citation that resolves a source id from the registry ARRAY (sources.sources)"
    - "Confidence, evidence and last-updated render as text, never colour alone, and the tokens are distinguishable out of context"
    - "The .fact wrapper is a block element, so it validly contains the details disclosure"
    - "Per-fact metadata uses progressive disclosure: brief inline tokens, with source/date/rationale (and checkedOn where present) behind a details element"
    - "Every confidence/evidence token links to a real Methodology page; links and summaries carry the GOV.UK focus treatment"
    - "A fixture page proves the trust component renders, and the build succeeds end to end through the prebuild gates"
  artifacts:
    - path: "src/_includes/components/macros.njk"
      provides: "The sourcedValue macro: block .fact wrapper, value + inline confidence/evidence text tokens with visually-hidden suffixes + details disclosure iterating the registry array"
      contains: "sourcedValue"
    - path: "src/methodology.njk"
      provides: "Methodology stub with #confidence and #evidence anchors and one clearly-illustrative example fact, the intended verification model, and the DATA-09 boundary"
      contains: "confidence"
    - path: "src/components-demo.njk"
      provides: "Fixture page rendering a fact through the trust component (a pa11y target)"
    - path: "src/assets/styles.css"
      provides: "Mobile-first accessible styles incl. .visually-hidden and the GOV.UK focus treatment"
      contains: "ffdd00"
  key_links:
    - from: "src/_includes/components/macros.njk"
      to: "src/_data/sources.json"
      via: "the macro resolves source ids with findBy over sources.sources (the array inside the object)"
      pattern: "findBy"
    - from: "src/_includes/components/macros.njk"
      to: "src/methodology.njk"
      via: "confidence/evidence tokens link to the methodology anchors"
      pattern: "/methodology/#confidence"
---

<objective>
Build the trust rendering component (TRUST-03/04): a single sourcedValue macro that renders a fact with its value, two inline colour-independent text tokens (confidence, evidence, each distinguishable out of context) and source/date/rationale behind a details element, resolving source ids against the registry ARRAY. Add the Methodology stub that anchors the tokens, a fixture demo page that proves the component renders, and the index and 404 pages so the site builds end to end through the prebuild gates.

Purpose: Progressive disclosure means a dense page is never four metadata items repeated inline on every value, and every badge links somewhere real from day one.
Output: the macro, a demo fixture fact, components-demo, methodology stub, index, 404, and minimal accessible styles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@./CLAUDE.md

<interfaces>
- /Users/anthonygeorge/Projects/DEBT/src/_includes/components/macros.njk (sourceNote, dataFreshnessBadge, caveatBox to extend)
- .eleventy.js filters from plan 01-01: findBy(arr,key,value), readableDate(value)
- src/_data/sources.json (an OBJECT {note, sources:[...]}; the macro resolves ids against the inner sources.sources ARRAY)
- src/_data/meta.json (confidenceLevels, evidenceLevels for the methodology anchors)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The sourcedValue trust macro and accessible styles</name>
  <files>src/_includes/components/macros.njk, src/assets/styles.css</files>
  <read_first>
    - 01-RESEARCH.md section "### Pattern 3: The trust rendering macro" (the exact macro contract sourcedValue(fact, sources, label, unit) where sources is the registry ARRAY; the block .fact div; the visually-hidden token suffixes; the details/summary disclosure resolving fact.sources via findBy; readableDate; the optional checkedOn and note; the GOV.UK focus mandate)
    - /Users/anthonygeorge/Projects/DEBT/src/_includes/components/macros.njk (the import convention and existing macros)
    - REQUIREMENTS.md TRUST-03, TRUST-04, SITE-04 (colour-independence)
  </read_first>
  <action>
    Create src/_includes/components/macros.njk mirroring DEBT's macro file and adding sourcedValue(fact, sources, label, unit) per the research Pattern 3 block. CRITICAL: the second argument is the registry ARRAY, not the sources.json object. sources.json is an object {note, sources:[...]} and DEBT's findBy returns undefined for a non-array, so the macro must iterate the array; callers pass sources.sources (see Tasks 2 and 3). Render: a BLOCK .fact wrapper as a <div> (NOT a <span> — a span cannot validly contain <details>/<ul>, and every Phase 3 page inherits this macro); the value (and unit); two inline text tokens reading "confidence {grade}" and "evidence {grade}", each a link to /methodology/#confidence and /methodology/#evidence, each carrying a visually-hidden suffix (e.g. " (curator certainty)" / " (evidence strength)") so the link text is distinguishable out of context (not a bare "confidence A"); and a <details> with summary "Source and date" listing each resolved source (findBy on the array by id, linking s.url with rel="noopener", showing s.jurisdiction where present), the readableDate of fact.updated, an optional "Checked on" date when fact.checkedOn is present (aligned with the envelope's checkedOn from plan 01-03), and fact.note. Tokens must be text, never colour alone. Keep Nunjucks autoescaping on; never pipe untrusted values through safe.
    Create src/assets/styles.css (this is the file the plan 01-01 passthrough copies to _site/assets/styles.css) with minimal mobile-first accessible styles: a .visually-hidden utility; the GOV.UK focus treatment on links and every <summary> (yellow #ffdd00 background, black text, a thick black bottom border / box-shadow), not merely a generic outline; 4.5:1 contrast; 44px touch targets; a skip-link style; and a .fact layout that does not rely on colour. No inline styles anywhere (so the tightened CSP style-src 'self' holds). British English, no em-dashes.
  </action>
  <verify>
    <automated>grep -q "macro sourcedValue" src/_includes/components/macros.njk && grep -q "/methodology/#confidence" src/_includes/components/macros.njk && grep -q "details" src/_includes/components/macros.njk && grep -q '<div class="fact"' src/_includes/components/macros.njk && grep -qi "ffdd00" src/assets/styles.css && grep -q "visually-hidden" src/assets/styles.css</automated>
  </verify>
  <acceptance_criteria>macros.njk defines sourcedValue with a block <div> .fact wrapper, inline text confidence/evidence tokens with visually-hidden suffixes linking to the methodology anchors, source resolution via findBy over the registry array, and a details disclosure for source/date/rationale (incl. checkedOn). No untrusted value is piped through safe. styles.css provides .visually-hidden and the GOV.UK #ffdd00 focus treatment and emits no inline styles.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Methodology stub, index and 404 pages</name>
  <files>src/methodology.njk, src/index.njk, src/404.njk</files>
  <read_first>
    - src/_data/meta.json (confidenceLevels and evidenceLevels to render as the anchored explanations)
    - src/_includes/layouts/base.njk (from plan 01-01; the layout to extend)
    - 01-RESEARCH.md section "### Pattern 3" final paragraph (methodology hosts the #confidence/#evidence anchors and one worked example fact; build the demo as a pa11y target)
    - REQUIREMENTS.md TRUST-04 (the anchors the tokens link to must exist from day one), DATA-09
  </read_first>
  <action>
    Create src/methodology.njk using base.njk: a stub Methodology page in plain GOV.UK-style English with a section id="confidence" rendering the confidenceLevels definitions from meta.json and a section id="evidence" rendering the evidenceLevels definitions, plus one example fact rendered through the sourcedValue macro (call it as sourcedValue(demoFact, sources.sources, ...)). HONESTY REQUIREMENTS (finding 30): label that example fact UNMISTAKABLY as illustrative / non-content (do NOT present its invented confidence/evidence grades as a real worked verification); describe the verification model as the INTENDED standard ("this is how facts are verified"), NEVER as already applied to all content; and mark the whole page clearly as a provisional foundation stub that a later phase (Phase 7) finalises. Include a short DATA-09 boundary note: a field is fact-bearing if and only if its schema uses the SourcedValue $ref, which enforces provenance only on fields already declared as the $ref and cannot, by itself, catch an empirical field mistakenly modelled as a bare scalar (the schema-consistency guard test does that). Create src/index.njk: replace the Plan 01-01 placeholder with a minimal accessible home page describing the project in neutral plain English and linking to Methodology. Create src/404.njk (permalink /404.html) with a helpful not-found message and a link home. All pages semantic, mobile-first, keyboard navigable, en-GB, no em-dashes (the editorial gate scans src/).
  </action>
  <verify>
    <automated>npm run build && test -f _site/methodology/index.html && grep -q 'id="confidence"' _site/methodology/index.html && grep -q 'id="evidence"' _site/methodology/index.html && test -f _site/404.html</automated>
  </verify>
  <acceptance_criteria>The build produces a Methodology page with #confidence and #evidence anchors rendering the meta.json definitions and one clearly-illustrative example fact, the intended (not already-applied) verification model, the DATA-09 boundary note, and a provisional-stub marker, plus an index and a 404 page. This verify runs full `npm run build`, proving the prebuild gates pass on the clean repo end to end.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Trust component demonstration fixture page</name>
  <files>src/_data/demoFact.json, src/components-demo.njk</files>
  <read_first>
    - src/_includes/components/macros.njk (the sourcedValue macro from Task 1)
    - schemas/sourced-value.schema.json (the demo fact must be a valid SourcedValue so it passes the gate)
    - src/_data/sources.json (a real source id for the demo fact to cite)
  </read_first>
  <action>
    Create src/_data/demoFact.json holding one provenance-complete SourcedValue fact pinned to claimType "authoritative" citing ONE real registry source id (or "corroborable" with TWO distinct real ids), with confidence and evidence from the GRADE enum, an updated date, and claimDomain "general" (NOT regulatory, so it does not trip the TRUST-06 GB-source + checkedOn rule and the full build stays green). Create src/components-demo.njk (permalink /components-demo/) using base.njk that imports the macro and renders demoFact through sourcedValue(demoFact, sources.sources, ...), demonstrating the inline tokens and the details disclosure. This page is a pa11y-ci target in plan 01-08. Add an explanatory heading making clear it is a component demonstration, not content. en-GB, no em-dashes.
  </action>
  <verify>
    <automated>npm run build && test -f _site/components-demo/index.html && grep -qi "confidence" _site/components-demo/index.html && grep -q "<details" _site/components-demo/index.html</automated>
  </verify>
  <acceptance_criteria>The demo page builds and renders the fixture fact through the trust macro, showing inline confidence/evidence tokens and a details disclosure with a non-empty resolved source list (proving the sources.sources array is passed). The demo fact is a valid SourcedValue with claimDomain "general", so the prebuild validation and TRUST-06 gates accept it and the full build is green. A one-off MANUAL keyboard + screen-reader (VoiceOver/NVDA) check of /components-demo/ is performed and recorded in the SUMMARY (pa11y is the automated floor, not proof of usability; it misses invalid nesting and focus quality — this is the GATE-02 early check for this component).</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data store to rendered HTML | The macro renders authored/ingested values into the browser under autoescaping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering / Elevation | Stored XSS via an ingested or external string rendered unescaped | mitigate | Nunjucks autoescaping left on; no safe filter on fact values or source names; jsonScript for any embedded JSON |
| T-07-02 | Information disclosure | Confidence/evidence conveyed by colour alone, or token text indistinguishable out of context, fails low-vision and screen-reader readers | mitigate | Tokens render as text with visually-hidden suffixes and link to Methodology; styles meet 4.5:1, do not rely on colour, and carry the GOV.UK focus treatment |
| T-07-03 | Tampering | A badge links to a non-existent Methodology anchor, or the source list renders silently empty | mitigate | methodology.njk ships the #confidence and #evidence anchors in this same plan; the macro iterates sources.sources so the resolved list is non-empty; build + demo verify both |
</threat_model>

<verification>
- `npm run build` succeeds end to end (prebuild gates pass), producing methodology, index, 404 and components-demo.
- The Methodology anchors exist and the demo renders the fact through the macro with a non-empty source list.
</verification>

<success_criteria>
The trust component renders every claim with text confidence/evidence tokens (distinguishable out of context) and progressive-disclosure provenance from the registry array, the .fact wrapper is a valid block element, the Methodology stub anchors the tokens and is honest about being a provisional intended-standard stub, a demo page proves it renders, and the whole site builds through the gates.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-07-SUMMARY.md` when done.
</output>
