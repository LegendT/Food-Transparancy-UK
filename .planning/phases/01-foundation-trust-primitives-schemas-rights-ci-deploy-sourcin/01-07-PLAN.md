---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 07
type: execute
wave: 5
depends_on: [01-03, 01-05, 01-06]
files_modified:
  - src/_includes/components/macros.njk
  - src/_data/demoFact.json
  - src/components-demo.njk
  - src/methodology.njk
  - src/index.njk
  - src/404.njk
  - src/assets/styles.css
autonomous: true
requirements: [TRUST-03, TRUST-04]
user_setup: []

must_haves:
  truths:
    - "A reader can see the source(s) behind a displayed claim via an inline citation that resolves a source id"
    - "Confidence, evidence and last-updated render as text, never colour alone"
    - "Per-fact metadata uses progressive disclosure: brief inline tokens, with source/date/rationale behind a details element"
    - "Every confidence/evidence token links to a real Methodology page"
    - "A fixture page proves the trust component renders, and the build succeeds end to end through the prebuild gates"
  artifacts:
    - path: "src/_includes/components/macros.njk"
      provides: "The sourcedValue macro: value + inline confidence/evidence text tokens + details disclosure"
      contains: "sourcedValue"
    - path: "src/methodology.njk"
      provides: "Methodology stub with #confidence and #evidence anchors and a worked example fact"
      contains: "confidence"
    - path: "src/components-demo.njk"
      provides: "Fixture page rendering a fact through the trust component (a pa11y target)"
  key_links:
    - from: "src/_includes/components/macros.njk"
      to: "src/_data/sources.json"
      via: "the macro resolves source ids with the findBy filter"
      pattern: "findBy"
    - from: "src/_includes/components/macros.njk"
      to: "src/methodology.njk"
      via: "confidence/evidence tokens link to the methodology anchors"
      pattern: "/methodology/#confidence"
---

<objective>
Build the trust rendering component (TRUST-03/04): a single sourcedValue macro that renders a fact with its value, two inline colour-independent text tokens (confidence, evidence) and source/date/rationale behind a details element, resolving source ids against the registry. Add the Methodology stub that anchors the tokens, a fixture demo page that proves the component renders, and the index and 404 pages so the site builds end to end through the prebuild gates.

Purpose: Progressive disclosure means a dense page is never four metadata items repeated inline on every value, and every badge links somewhere real from day one.
Output: the macro, a demo fixture fact, components-demo, methodology stub, index, 404, and minimal accessible styles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@./CLAUDE.md

<interfaces>
- /Users/anthonygeorge/Projects/DEBT/src/_includes/components/macros.njk (sourceNote, dataFreshnessBadge, caveatBox to extend)
- .eleventy.js filters from plan 01-01: findBy(arr,key,value), readableDate(value)
- src/_data/sources.json (the registry the macro resolves ids against)
- src/_data/meta.json (confidenceLevels, evidenceLevels for the methodology anchors)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The sourcedValue trust macro</name>
  <files>src/_includes/components/macros.njk, src/assets/styles.css</files>
  <read_first>
    - 01-RESEARCH.md section "### Pattern 3: The trust rendering macro" (the exact macro contract sourcedValue(fact, sources, label, unit): inline confidence/evidence text tokens linking to /methodology/#confidence and #evidence, the details/summary disclosure resolving fact.sources via findBy, readableDate for dates, the optional checkedOn and note)
    - /Users/anthonygeorge/Projects/DEBT/src/_includes/components/macros.njk (the import convention and existing macros)
    - REQUIREMENTS.md TRUST-03, TRUST-04, SITE-04 (colour-independence)
  </read_first>
  <action>
    Create src/_includes/components/macros.njk mirroring DEBT's macro file and adding sourcedValue(fact, sources, label, unit) per the research Pattern 3 block: render the value (and unit), two inline text tokens reading "confidence {grade}" and "evidence {grade}" each as a link to /methodology/#confidence and /methodology/#evidence, and a details element with summary "Source and date" listing each resolved source (findBy on the sources array by id, linking s.url with rel="noopener", showing s.jurisdiction where present), the readableDate of fact.updated, an optional "Checked on" date, and fact.note. The tokens must be text, never colour alone. Keep Nunjucks autoescaping on; never pipe untrusted values through safe. Also bring across DEBT's dataFreshnessBadge, sourceNote and caveatBox where useful. Create src/assets/styles.css with minimal mobile-first accessible styles: visible focus indicators, 4.5:1 contrast, 44px touch targets, a skip-link style, and a .fact layout that does not rely on colour. British English, no em-dashes.
  </action>
  <verify>
    <automated>grep -q "macro sourcedValue" src/_includes/components/macros.njk && grep -q "/methodology/#confidence" src/_includes/components/macros.njk && grep -q "details" src/_includes/components/macros.njk</automated>
  </verify>
  <acceptance_criteria>macros.njk defines sourcedValue with inline text confidence/evidence tokens linking to the methodology anchors, source resolution via findBy, and a details disclosure for source/date/rationale. No untrusted value is piped through safe.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Methodology stub, index and 404 pages</name>
  <files>src/methodology.njk, src/index.njk, src/404.njk</files>
  <read_first>
    - src/_data/meta.json (confidenceLevels and evidenceLevels to render as the anchored explanations)
    - src/_includes/layouts/base.njk (from plan 01-01; the layout to extend)
    - 01-RESEARCH.md section "### Pattern 3" final paragraph (methodology hosts the #confidence/#evidence anchors and one worked example fact; build the demo as a pa11y target)
    - REQUIREMENTS.md TRUST-04 (the anchors the tokens link to must exist from day one)
  </read_first>
  <action>
    Create src/methodology.njk using base.njk: a stub Methodology page in plain GOV.UK-style English with a section id="confidence" rendering the confidenceLevels definitions from meta.json and a section id="evidence" rendering the evidenceLevels definitions, plus one worked example fact rendered through the sourcedValue macro so the page dogfoods the component. Mark it clearly as a foundation stub that Phase 7 finalises. Create src/index.njk: a minimal accessible home page describing the project in neutral plain English and linking to Methodology. Create src/404.njk (permalink /404.html) with a helpful not-found message and a link home. All pages semantic, mobile-first, keyboard navigable, en-GB, no em-dashes (the editorial gate scans src/).
  </action>
  <verify>
    <automated>npm run build && test -f _site/methodology/index.html && grep -q 'id="confidence"' _site/methodology/index.html && grep -q 'id="evidence"' _site/methodology/index.html && test -f _site/404.html</automated>
  </verify>
  <acceptance_criteria>The build produces a Methodology page with #confidence and #evidence anchors rendering the meta.json definitions and a worked example fact, plus an index and a 404 page. This verify runs full `npm run build`, proving the prebuild gates pass on the clean repo end to end.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Trust component demonstration fixture page</name>
  <files>src/_data/demoFact.json, src/components-demo.njk</files>
  <read_first>
    - src/_includes/components/macros.njk (the sourcedValue macro from Task 1)
    - schemas/sourced-value.schema.json (the demo fact must be a valid SourcedValue so it passes the gate)
    - src/_data/sources.json (a real source id for the demo fact to cite)
  </read_first>
  <action>
    Create src/_data/demoFact.json holding one provenance-complete SourcedValue fact that cites a real registry source id (so the validation gate accepts it) with confidence and evidence from the GRADE enum, an updated date, and a claimType. Create src/components-demo.njk (permalink /components-demo/) using base.njk that imports the macro and renders demoFact through sourcedValue, demonstrating the inline tokens and the details disclosure. This page is a pa11y-ci target in plan 01-08. Add an explanatory heading making clear it is a component demonstration, not content. en-GB, no em-dashes.
  </action>
  <verify>
    <automated>npm run build && test -f _site/components-demo/index.html && grep -qi "confidence" _site/components-demo/index.html && grep -q "<details" _site/components-demo/index.html</automated>
  </verify>
  <acceptance_criteria>The demo page builds and renders the fixture fact through the trust macro, showing inline confidence/evidence tokens and a details disclosure. The demo fact is a valid SourcedValue, so the prebuild validation gate accepts it.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data store to rendered HTML | The macro renders authored/ingested values into the browser under autoescaping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering / Elevation | Stored XSS via an ingested or external string rendered unescaped | mitigate | Nunjucks autoescaping left on; no safe filter on fact values or source names; jsonScript for any embedded JSON |
| T-07-02 | Information disclosure | Confidence/evidence conveyed by colour alone fails low-vision readers | mitigate | Tokens render as text and link to Methodology; styles meet 4.5:1 and do not rely on colour |
| T-07-03 | Tampering | A badge links to a non-existent Methodology anchor | mitigate | methodology.njk ships the #confidence and #evidence anchors in this same plan; build verifies they exist |
</threat_model>

<verification>
- `npm run build` succeeds end to end (prebuild gates pass), producing methodology, index, 404 and components-demo.
- The Methodology anchors exist and the demo renders the fact through the macro.
</verification>

<success_criteria>
The trust component renders every claim with text confidence/evidence tokens and progressive-disclosure provenance, the Methodology stub anchors the tokens, a demo page proves it renders, and the whole site builds through the gates.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-07-SUMMARY.md` when done.
</output>
