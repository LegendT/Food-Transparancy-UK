---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 08
type: execute
wave: 6
depends_on: [01-05, 01-06, 01-07]
files_modified:
  - .pa11yci.json
  - .github/workflows/ci.yml
autonomous: false
requirements: [INFRA-01]
user_setup:
  - service: netlify
    why: "Static deploy with CSP so every gate has a host from day one (INFRA-01)"
    dashboard_config:
      - task: "Create the Netlify site and connect the git repository"
        location: "Netlify dashboard -> Add new site -> Import from Git"
  - service: github
    why: "CI runs the prebuild gates, the negative-fixture suite and pa11y-ci on every push"
    dashboard_config:
      - task: "Confirm GitHub Actions is enabled for the repository"
        location: "GitHub repo -> Settings -> Actions"

must_haves:
  truths:
    - "pa11y-ci passes WCAG 2.2 AA across the Phase 1 routes (home, methodology, 404, components-demo)"
    - "A CI pipeline runs the prebuild gates, the negative-fixture suite and pa11y-ci on every push"
    - "The Netlify static deploy runs npm run build, so the prebuild gates gate the deploy"
  artifacts:
    - path: ".pa11yci.json"
      provides: "WCAG2AA config listing the Phase 1 routes with the no-sandbox chrome args"
      contains: "WCAG2AA"
    - path: ".github/workflows/ci.yml"
      provides: "CI: install, prebuild gates, npm test, build, a11y:ci"
      contains: "prebuild"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts"
      via: "CI invokes prebuild, test, build and a11y:ci"
      pattern: "npm"
---

<objective>
Stand up the CI/deploy substrate so every gate has a host from day one (INFRA-01): a pa11y-ci config over the Phase 1 routes, a GitHub Actions pipeline that runs the prebuild gates plus the negative-fixture suite plus pa11y-ci on every push, and the Netlify static deploy whose build command runs the gates. The Netlify site connection and Actions enablement are the only genuinely human steps.

Purpose: Gates enforced in CI and at deploy time, not as a promise enforced later.
Output: .pa11yci.json, .github/workflows/ci.yml, a connected Netlify deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@./CLAUDE.md

<interfaces>
- /Users/anthonygeorge/Projects/DEBT/.pa11yci.json (the WCAG2AA defaults, timeout, no-sandbox chromeLaunchConfig to mirror)
- package.json scripts from plan 01-01: prebuild, build, test, a11y:serve, a11y:ci, a11y:all
- netlify.toml from plan 01-01 (build command npm run build, NODE_VERSION 24)
- src/ pages from plan 01-07: /, /methodology/, /404.html, /components-demo/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pa11y-ci config and the accessibility floor</name>
  <files>.pa11yci.json</files>
  <read_first>
    - /Users/anthonygeorge/Projects/DEBT/.pa11yci.json (the defaults block: standard WCAG2AA, timeout, wait, chromeLaunchConfig args no-sandbox/disable-setuid-sandbox; the urls array to replace)
    - 01-RESEARCH.md "### Recommended Project Structure" (.pa11yci.json Phase 1 urls: /, /methodology/, /404.html, /components-demo/)
    - REQUIREMENTS.md SITE-04, INFRA-01
  </read_first>
  <action>
    Create .pa11yci.json mirroring DEBT's defaults (standard WCAG2AA, timeout 30000, wait, the no-sandbox chromeLaunchConfig args for CI) with the urls array set to the four Phase 1 routes served on 127.0.0.1:8081: the home /, /methodology/, /404.html and /components-demo/. Then run the local a11y suite to prove the floor passes.
  </action>
  <verify>
    <automated>npm run a11y:all</automated>
  </verify>
  <acceptance_criteria>`npm run a11y:all` builds, serves _site and passes pa11y-ci WCAG 2.2 AA across the four Phase 1 routes. .pa11yci.json carries the no-sandbox args so CI can run headless.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions CI pipeline running the gates</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - 01-RESEARCH.md section "### System Architecture Diagram" and the paragraph after it (CI runs the four prebuild gates plus npm test plus a11y:ci on every push/PR)
    - 01-RESEARCH.md "## Open Questions" item 3 (GitHub Actions for the gate/test/a11y pipeline; Netlify build runs prebuild regardless)
    - package.json scripts (from plan 01-01)
    - REQUIREMENTS.md INFRA-01
  </read_first>
  <action>
    Create .github/workflows/ci.yml triggered on push and pull_request. One job on ubuntu-latest: checkout, setup-node pinned to Node 24 with npm cache, npm ci, then run npm run prebuild (the four gates: validate, lint:editorial, check:images), npm test (the negative-fixture suite), npm run build, and npm run a11y:ci against a served _site (use the a11y:all script or an explicit serve-then-test step). Headless Chrome runs with the no-sandbox args already in .pa11yci.json. Fail the job on any non-zero step so a bad commit cannot merge. British English in comments, no em-dashes.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); ['npm ci','prebuild','npm test','run build','a11y'].forEach(t=>{if(!y.includes(t))process.exit(1)}); if(!/node-version[^\n]*24/.test(y))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>ci.yml runs on push and PR, pins Node 24, and executes prebuild, npm test, build and the a11y check, failing on any error. The negative-fixture suite and the four gates all run in CI.</acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Connect the Netlify deploy and confirm the gates gate the deploy</name>
  <read_first>
    - netlify.toml (from plan 01-01; build command npm run build, publish _site, NODE_VERSION 24, the tightened CSP)
    - 01-RESEARCH.md "## Runtime State Inventory" (Netlify site to be created; no pre-existing config) and "## Environment Availability"
  </read_first>
  <what-built>Claude has produced netlify.toml (plan 01-01) with the build command and CSP, the pa11y config, and the CI pipeline. The remaining step needs a human: creating the Netlify site and connecting the git repository. This cannot be done by Claude without account access.</what-built>
  <how-to-verify>
    1. In the Netlify dashboard, add a new site and import the git repository.
    2. Confirm Netlify reads netlify.toml: build command npm run build, publish directory _site, NODE_VERSION 24.
    3. Trigger a deploy and confirm it runs the prebuild gates (the build log shows validate, lint:editorial, check:images) before Eleventy.
    4. Confirm the deployed site responds with the tightened CSP header (no cloudflareinsights allowances) and the security headers.
    5. Optional sanity check: push a commit with a deliberately bad fixture on a throwaway branch and confirm the deploy fails at the prebuild gate, then discard it.
  </how-to-verify>
  <resume-signal>Type "deployed" once the site is live and the build log shows the prebuild gates running, or describe any failure.</resume-signal>
  <acceptance_criteria>The Netlify site is connected, a deploy succeeds running the prebuild gates before Eleventy, and the served headers include the tightened CSP. Every gate now has a host from day one (INFRA-01).</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo to CI runner | CI executes repo scripts; a poisoned script or dependency runs here |
| CI/Netlify to public | The deploy publishes only what passed the gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | A bad commit deploys because the gates run only in tests | mitigate | Netlify build command is npm run build, so prebuild gates run at deploy time; CI also runs them on every push |
| T-08-02 | Tampering | Supply-chain code runs in CI | mitigate | npm ci against the committed lockfile; no npx of unverified packages; pinned Node 24 |
| T-08-03 | Information disclosure | CI secrets leaked | accept (low) | Phase 1 needs no deploy secrets beyond the Netlify build token managed in the Netlify UI; none committed to the repo |
| T-08-04 | Tampering | Weak CSP allows injected scripts | mitigate | Tightened CSP from plan 01-01 verified on the live deploy in Task 3 |
</threat_model>

<verification>
- `npm run a11y:all` passes across the four routes.
- ci.yml runs the gates, the test suite, the build and the a11y check on Node 24.
- The Netlify deploy runs the prebuild gates and serves the tightened CSP (human-confirmed).
</verification>

<success_criteria>
The accessibility floor passes, CI enforces the gates and the negative-fixture suite on every push, and the Netlify deploy runs the gates so every gate has a host from day one.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-08-SUMMARY.md` when done.
</output>
