---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 09
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - docs/SELECTION-CRITERIA.md
  - docs/sourcing-backlog.json
  - schemas/sourcing-backlog.schema.json
  - test/backlog.test.js
autonomous: false
requirements: [PROD-14]
user_setup: []

must_haves:
  truths:
    - "The named corpus / Tier A selection rubric is recorded before any content, with a one-line rationale per target"
    - "A sourcing backlog of at least 20 CANDIDATE targets across tiers A/B/C exists in the project data (a pool the spike winnows, not 20 confirmed Tier A), each with an assigned documented driver, a tier hint and a one-line rationale"
    - "The backlog is not all-Tier-A-candidate, so it cannot silently over-promise against the project's own ~15-20 Tier A feasibility ceiling"
    - "Selection is by recorded named criteria, never a computed transformation metric"
    - "The backlog is machine-verifiable: a test asserts the count, the driver, the tier hint and the rationale per target"
  artifacts:
    - path: "docs/SELECTION-CRITERIA.md"
      provides: "The named PROD-14 rubric (UK ubiquity, flagship-category coverage, Tier A sourceability) with rationale"
    - path: "docs/sourcing-backlog.json"
      provides: "At least 20 candidate targets across tiers A/B/C, each with driver, tier hint and one-line rationale"
      contains: "driver"
    - path: "test/backlog.test.js"
      provides: "Asserts >=20 candidate targets, each with a driver, a tier hint and a rationale, valid against the schema, and that the pool is not all-Tier-A-candidate"
  key_links:
    - from: "test/backlog.test.js"
      to: "docs/sourcing-backlog.json"
      via: "the test loads and asserts the backlog structure and count"
      pattern: "sourcing-backlog"
---

<objective>
Record the named corpus / Tier A selection rubric (PROD-14) and a machine-verifiable sourcing backlog of at least 20 CANDIDATE targets across tiers A/B/C (a pool the spike winnows, NOT 20 confirmed-sourceable Tier A), each with an assigned documented driver, a tier hint and a one-line selection rationale, before any content exists. Selection is by recorded named criteria, never a computed transformation metric. This is editorial work: Claude drafts a candidate from the documented drivers, and a human editor confirms the selection.

Purpose: The historic-sourcing track opens here; the backlog is the long-pole work queue and the PROD-14 contract the corpus is held to. Framing the pool as tiered candidates keeps it honest about the project's own ~15-20 Tier A feasibility ceiling.
Output: docs/SELECTION-CRITERIA.md, docs/sourcing-backlog.json, its schema, and a test asserting the backlog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the named selection rubric and draft the sourcing backlog</name>
  <files>docs/SELECTION-CRITERIA.md, docs/sourcing-backlog.json, schemas/sourcing-backlog.schema.json</files>
  <read_first>
    - ROADMAP.md "## Workstreams" Historic-sourcing track (the documented drivers: the 2015 ice-cream compositional rule, the 2018 Soft Drinks Industry Levy, the FSA salt 2006+ / sugar 2015+ reduction programmes; the flagship anchor categories ice cream, soft drinks, chocolate, bread; the >=20-target exit requirement)
    - REQUIREMENTS.md PROD-14 (named criteria: UK market ubiquity, coverage across flagship anchor categories, sourceability of a historical formulation for Tier A; one-line rationale per product; never a computed transformation metric)
    - 01-RESEARCH.md section "### Recommended Project Structure" (docs/SELECTION-CRITERIA.md + docs/sourcing-backlog.json as machine-verifiable artefacts)
  </read_first>
  <action>
    Create docs/SELECTION-CRITERIA.md stating the named rubric in plain en-GB: the criteria (UK market ubiquity, coverage across the flagship anchor categories ice cream / soft drinks / chocolate / bread, and sourceability of a historical formulation for Tier A), how Tier A versus Tier B/C is decided, and the explicit rule that selection is editorial and never a computed transformation metric. Create schemas/sourcing-backlog.schema.json describing a targets array where each item requires a name, a category (one of the flagship anchor categories), a driver object (type mandate or incentive, and name referencing a documented driver such as the 2018 SDIL or the FSA sugar programme), a tier hint (an enum of A-candidate / B-candidate / C-candidate), and a one-line rationale string. Create docs/sourcing-backlog.json with at least 20 CANDIDATE targets across tiers A/B/C drawn from the documented drivers and anchor categories, each with its assigned driver, tier hint and a one-line rationale. These are CANDIDATES the SPIKE-01 spike winnows, NOT 20 confirmed-sourceable Tier A: the project's own feasibility ceiling is roughly 15-20 Tier A across the WHOLE project, so the pool must span tiers and must not be all-Tier-A-candidate. This is a drafted candidate for editorial confirmation in Task 3. No em-dashes (docs is scanned by the editorial gate). AUTHORING NOTE (rule-explaining prose): docs/SELECTION-CRITERIA.md is analyst-scope prose that explains the neutrality / no-metric rule; if explaining a rule requires NAMING a banned Class B term, wrap that line in the `<!-- editorial-allow: quote -->` directive (or avoid the literal token), or the editorial gate (plan 01-06) will fail the build.
  </action>
  <verify>
    <automated>node -e "const b=require('./docs/sourcing-backlog.json'); const t=b.targets||b; if(!Array.isArray(t)||t.length<20)process.exit(1); for(const x of t){if(!x.driver||!x.driver.type||!x.rationale||!x.tier)process.exit(1)} if(t.every(x=>String(x.tier).startsWith('A')))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>SELECTION-CRITERIA.md records the named rubric and the no-metric rule; sourcing-backlog.json holds at least 20 candidate targets across tiers A/B/C, each with a driver (type plus documented name), a tier hint and a one-line rationale, the pool is not all-Tier-A-candidate, and every record is valid against sourcing-backlog.schema.json.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Machine-verifiable backlog test</name>
  <files>test/backlog.test.js</files>
  <behavior>
    - The test asserts the backlog has at least 20 candidate targets.
    - Each target has a driver (type mandate or incentive plus a name), a tier hint (A/B/C candidate) and a non-empty one-line rationale.
    - The backlog is NOT all-Tier-A-candidate (at least one non-A-candidate present), so it cannot silently over-promise.
    - Each target validates against sourcing-backlog.schema.json.
  </behavior>
  <read_first>
    - 01-VALIDATION.md "## Manual-Only Verifications" (PROD-14 row: a structured backlog test asserts >=20 targets each with a driver + rationale)
    - /Users/anthonygeorge/Projects/DEBT/test/data.test.js (the node:test load-and-assert pattern)
    - docs/sourcing-backlog.json and schemas/sourcing-backlog.schema.json (from Task 1)
  </read_first>
  <action>
    Create test/backlog.test.js using node:test: load docs/sourcing-backlog.json, assert at least 20 candidate targets, assert each carries a driver with a valid type (mandate or incentive) and a name, a tier hint (A/B/C candidate) and a non-empty rationale, assert the pool is NOT all-Tier-A-candidate (so it cannot silently over-promise against the ~15-20 Tier A feasibility ceiling), and assert each target validates against sourcing-backlog.schema.json (reuse lib/validate.mjs from plan 01-05 if available, or compile the single schema with Ajv directly). British English, no em-dashes.
  </action>
  <verify>
    <automated>node --test test/backlog.test.js</automated>
  </verify>
  <acceptance_criteria>test/backlog.test.js passes, asserting the backlog count, the per-target driver, tier hint and rationale, that the pool is not all-Tier-A-candidate, and schema validity. PROD-14 is now machine-verifiable.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Editorial confirmation of the selection</name>
  <read_first>
    - docs/SELECTION-CRITERIA.md and docs/sourcing-backlog.json (the drafted rubric and backlog)
  </read_first>
  <what-built>Claude has drafted the named selection rubric and a backlog of at least 20 candidate targets across tiers A/B/C, each tied to a documented driver with a tier hint and a one-line rationale, all passing the structural test. The selection itself is editorial judgement and needs the editor's confirmation.</what-built>
  <how-to-verify>
    1. Read docs/SELECTION-CRITERIA.md and confirm the named criteria match the editorial intent and contain no transformation-metric ranking.
    2. Read docs/sourcing-backlog.json and confirm the 20+ candidate targets, their tier hints, assigned drivers and one-line rationales are sound and genuinely flagship, and that the pool honestly spans tiers rather than over-promising 20 Tier A.
    3. Adjust any target, tier hint, driver or rationale as needed, then approve.
  </how-to-verify>
  <resume-signal>Type "approved" once the rubric and backlog reflect the editorial selection, or list the changes to make.</resume-signal>
  <acceptance_criteria>The editor has confirmed the rubric and the 20+ candidate-target backlog. The recorded selection is editorial, by named criteria, with a per-target tier hint, rationale and driver (PROD-14), and the pool is honestly tiered.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editorial judgement to recorded data | The rubric and backlog encode selection decisions that later phases are held to |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Selection drifts to a covert transformation-metric ranking (anti-brand framing) | mitigate | SELECTION-CRITERIA.md records named criteria and the explicit no-metric rule; the backlog stores driver + tier hint + rationale, not a computed score |
| T-09-02 | Repudiation | A target lacks a documented driver or rationale, weakening the audit trail | mitigate | backlog.test.js fails the build path if any target is missing a driver, tier hint or rationale |
| T-09-03 | Repudiation | The backlog silently over-promises 20 Tier A against a ~15-20 project ceiling | mitigate | Targets are framed as tiered candidates; backlog.test.js asserts the pool is not all-Tier-A-candidate |
</threat_model>

<verification>
- `node --test test/backlog.test.js` passes.
- The rubric records named criteria and the no-metric rule; the editor has confirmed the selection; the pool is honestly tiered.
</verification>

<success_criteria>
The named corpus / Tier A rubric and a machine-verifiable backlog of at least 20 driver-tagged, tier-hinted, rationale-bearing candidate targets across tiers A/B/C exist before any content, not all-Tier-A-candidate, confirmed by the editor (PROD-14).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-09-SUMMARY.md` when done.
</output>
