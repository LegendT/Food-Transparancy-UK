---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 10
type: execute
wave: 5
depends_on: [01-04, 01-05, 01-09]
files_modified:
  - src/_data/products/spike-01.json
  - src/_data/products/spike-02.json
  - src/_data/products/spike-03.json
  - src/_data/timeline/spike-events.json
  - docs/SPIKE-01-FINDINGS.md
  - docs/spike-findings.json
  - schemas/spike-findings.schema.json
  - test/spike.test.js
autonomous: false
requirements: [SPIKE-01]
user_setup: []

must_haves:
  truths:
    - "Three Tier A products are taken fully end to end: historic formulation, two-pass verification, assigned driver"
    - "The real per-product effort and the dead-end rate are recorded"
    - "The launch-corpus target (PROD-12) and the Phase 4 Tier A entry gate are re-derived from the spike evidence"
    - "The three records dogfood the schema gate: they validate and pass the build"
  artifacts:
    - path: "src/_data/products/spike-01.json"
      provides: "A real Tier A product record with sourced historical formulation, valid against the schema"
      contains: "claimType"
    - path: "docs/spike-findings.json"
      provides: "Recorded effort, dead-end rate, and re-derived corpus target + Tier A entry gate"
      contains: "rederived"
    - path: "test/spike.test.js"
      provides: "Asserts 3 products, effort + dead-end + re-derived figures, and schema validity of the records"
  key_links:
    - from: "src/_data/products/spike-01.json"
      to: "schemas/product.schema.json"
      via: "the spike records validate through the prebuild gate"
      pattern: "sources"
    - from: "docs/spike-findings.json"
      to: ".planning/ROADMAP.md"
      via: "the re-derived PROD-12 target and Tier A entry gate feed Phase 4 scoping"
      pattern: "rederived"
---

<objective>
Run the SPIKE-01 sourcing spike: take three Tier A products fully end to end (historic formulation plus two-pass verification plus assigned driver), record the real per-product effort and the dead-end rate, and re-derive the launch-corpus target (PROD-12) and the Phase 4 Tier A entry gate from that evidence. The three product records dogfood the schema gate, proving the data model works on real content. This is manual historic-sourcing and verification work, so it is autonomous false.

Purpose: The corpus scope is evidence-derived, not asserted upfront. This spike produces the first hard evidence.
Output: three real product records plus their timeline events, SPIKE-01-FINDINGS.md, spike-findings.json, its schema, and a test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-VALIDATION.md
@./CLAUDE.md

<interfaces>
- schemas/product.schema.json, schemas/timeline-event.schema.json (from plan 01-04; the records must validate)
- schemas/sourced-value.schema.json, schemas/date-value.schema.json (the envelope and dates)
- src/_data/sources.json (records must cite real registry source ids; add new sources as needed)
- docs/sourcing-backlog.json (from plan 01-09; the three spike products are drawn from this backlog)
- scripts/validate-data.mjs (from plan 01-05; the gate the records must pass)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Source and two-pass verify three Tier A products end to end</name>
  <read_first>
    - ROADMAP.md "## Workstreams" Historic-sourcing track (the SPIKE-01 exit: three fully verified Tier A products plus a re-derived corpus/entry-gate figure plus the >=20 backlog) and PROJECT.md Constraints "Two-pass verification"
    - docs/sourcing-backlog.json (the candidate flagship targets to draw the three from)
    - schemas/product.schema.json, schemas/timeline-event.schema.json, schemas/sourced-value.schema.json (the shapes the records must satisfy)
  </read_first>
  <what-built>This task is the manual editorial spike: choosing three Tier A products from the backlog, hand-sourcing each historical formulation, running the two-pass verification the claim type demands (corroborable facts: two distinct-lineage sources, at least one primary; authoritative facts: one authority plus an independent re-read), assigning the documented driver, and recording the real per-product effort and the dead-end rate encountered. Claude can assist with drafting and structuring, but the sourcing and verification are human editorial work.</what-built>
  <how-to-verify>
    1. Select three Tier A products from docs/sourcing-backlog.json spanning the flagship categories.
    2. For each, source the historical and current formulation from primary/authoritative sources, recording each source in src/_data/sources.json with its jurisdiction and licence.
    3. Run the two-pass verification for each fact and record the verdicts.
    4. Note the per-product effort (time/passes) and any dead ends (targets that could not be sourced to Tier A).
    5. Confirm the editorial sourcing is genuinely complete for all three before the records are written.
  </how-to-verify>
  <resume-signal>Type "sourced" once three products are fully sourced and two-pass verified with effort and dead-ends noted, or describe blockers.</resume-signal>
  <acceptance_criteria>Three Tier A products have been hand-sourced and two-pass verified, their sources recorded in the registry, and the per-product effort and dead-end rate noted, ready to encode as records and findings.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Encode the three product records and timeline events</name>
  <files>src/_data/products/spike-01.json, src/_data/products/spike-02.json, src/_data/products/spike-03.json, src/_data/timeline/spike-events.json</files>
  <read_first>
    - schemas/product.schema.json and schemas/timeline-event.schema.json (from plan 01-04)
    - src/_data/sources.json (the registry the records cite, updated in Task 1)
    - 01-RESEARCH.md section "## Architectural Responsibility Map" (the SPIKE-01 row: 3 real entity records dogfood the schema gate)
  </read_first>
  <action>
    Encode the three sourced products as src/_data/products/spike-01.json, spike-02.json and spike-03.json, each a valid product record: fact-bearing fields (current and historical formulation, manufacturer, nutrition where sourced, structured allergens) as SourcedValue envelopes citing the real registry source ids, with confidence and evidence from the GRADE enum, updated dates, and the correct claimType (corroborable for empirical formulation facts with two sources, authoritative for label/regulatory facts). Encode the sourced formulation-change events in src/_data/timeline/spike-events.json as TimelineEvent records with ranged/circa dates, the three-way documentedChange / statedReason / labelledInference separation, and the assigned documented driver. Every record must pass scripts/validate-data.mjs (the prebuild gate). British English, no em-dashes, no imputation of motive as fact.
  </action>
  <verify>
    <automated>node scripts/validate-data.mjs && node -e "const fs=require('fs'); ['spike-01','spike-02','spike-03'].forEach(n=>{if(!fs.existsSync('src/_data/products/'+n+'.json'))process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>The three product records and the timeline events exist, cite real registry sources, and pass the validation gate (`node scripts/validate-data.mjs` exits 0). The records dogfood the full schema on real content.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Findings, re-derived figures and the spike test</name>
  <files>docs/SPIKE-01-FINDINGS.md, docs/spike-findings.json, schemas/spike-findings.schema.json, test/spike.test.js</files>
  <behavior>
    - spike-findings.json records three products with per-product effort, the dead-end rate, and the re-derived PROD-12 corpus target and Tier A entry gate.
    - test/spike.test.js asserts three products, the presence of effort + dead-end + re-derived figures, and that each spike product record validates against product.schema.json.
  </behavior>
  <read_first>
    - ROADMAP.md cross-phase data dependencies (SPIKE-01 re-derives PROD-12 and the Phase 4 Tier A entry gate; the provisional figures ~100 corpus and >=15 Tier A are placeholders the spike replaces)
    - 01-VALIDATION.md "## Manual-Only Verifications" (SPIKE-01 row: spike-findings.json test asserts 3 products with effort + dead-end + re-derived figures; the 3 records dogfood the gate)
    - src/_data/products/spike-0*.json (from Task 2)
  </read_first>
  <action>
    Create schemas/spike-findings.schema.json describing the findings: a products array of three entries each with an effort measure and an outcome; a deadEndRate field; and a rederived object with corpusTarget (the re-derived PROD-12 figure) and tierAEntryGate (the re-derived Phase 4 threshold). Create docs/spike-findings.json populated from the Task 1 evidence and the Task 2 records, and docs/SPIKE-01-FINDINGS.md narrating the spike, the effort, the dead ends, and the reasoning behind the re-derived figures in plain en-GB. Create test/spike.test.js asserting: exactly three products in the findings, each with a recorded effort; a present deadEndRate; a rederived block with corpusTarget and tierAEntryGate; and that each src/_data/products/spike-0*.json validates against product.schema.json (via lib/validate.mjs or a direct Ajv compile). No em-dashes.
  </action>
  <verify>
    <automated>node --test test/spike.test.js</automated>
  </verify>
  <acceptance_criteria>test/spike.test.js passes: three products with effort, a dead-end rate, and re-derived corpus target and Tier A entry gate are recorded, and the three product records validate against the schema. SPIKE-01 is machine-verifiable and the corpus scope is now evidence-derived.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hand-sourced historic data to canonical store | The first real content enters the dataset and must pass the same gates as all future content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | A fabricated or under-sourced historical formulation published as fact | mitigate | Two-pass verification in Task 1; the records pass the provenance and claim-type gate (plan 01-05); corroborable formulation facts require two distinct-lineage sources |
| T-10-02 | Tampering / Elevation | Motive imputed as fact in a change narrative (defamation) | mitigate | TimelineEvent keeps statedReason and labelledInference separate; the editorial lint (plan 01-06) bans motive verbs; no imputation of motive |
| T-10-03 | Repudiation | The re-derived corpus figures are asserted without recorded evidence | mitigate | spike-findings.json records per-product effort and dead-end rate behind the re-derived figures; test asserts their presence |
</threat_model>

<verification>
- `node scripts/validate-data.mjs` exits 0 with the three spike records present (they pass the gate).
- `node --test test/spike.test.js` passes.
- The re-derived PROD-12 target and Tier A entry gate are recorded for Phase 4 scoping.
</verification>

<success_criteria>
Three Tier A products are sourced, two-pass verified and encoded as schema-valid records, the per-product effort and dead-end rate are recorded, and the launch-corpus target and Phase 4 Tier A entry gate are re-derived from the spike evidence (SPIKE-01).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-10-SUMMARY.md` when done.
</output>
