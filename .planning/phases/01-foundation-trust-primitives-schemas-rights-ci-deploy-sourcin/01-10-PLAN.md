---
phase: 01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin
plan: 10
type: execute
wave: 5
depends_on: [01-02, 01-04, 01-05, 01-09]
files_modified:
  - src/_data/sources.json
  - src/_data/products/spike-01.json
  - src/_data/products/spike-02.json
  - src/_data/products/spike-03.json
  - src/_data/timeline/spike-events.json
  - docs/SPIKE-01-FINDINGS.md
  - docs/spike-findings.json
  - schemas/spike-findings.schema.json
  - test/spike.test.js
autonomous: false
requirements: [SPIKE-01]
user_setup: []

must_haves:
  truths:
    - "Three Tier A products are taken fully end to end: historic formulation, a MANUAL two-pass verification dry-run, assigned driver"
    - "The real per-product effort and the dead-end rate (with its attempt denominator) are recorded"
    - "The launch-corpus target (PROD-12) and the Phase 4 Tier A entry gate are re-derived as PROVISIONAL estimates from n=3, explicitly placeholders Phase 4 refines"
    - "The two-pass verification is a recorded editorial dry-run, NOT gate-enforced in Phase 1; the spike names the requirements the Phase 2 automated gate must enforce"
    - "The three records dogfood the schema gate: they validate and pass the build"
  artifacts:
    - path: "src/_data/products/spike-01.json"
      provides: "A real Tier A product record with sourced historical formulation, valid against the schema"
      contains: "claimType"
    - path: "docs/spike-findings.json"
      provides: "Recorded attempts, per-product outcome, effort, dead-end rate (with denominator), and provisional re-derived corpus target + Tier A entry gate"
      contains: "rederived"
    - path: "test/spike.test.js"
      provides: "Asserts 3 products, attempts/effort/dead-end consistency, provisional re-derived figures, and schema validity of the records"
  key_links:
    - from: "src/_data/products/spike-01.json"
      to: "schemas/product.schema.json"
      via: "the spike records validate through the prebuild gate"
      pattern: "sources"
    - from: "docs/spike-findings.json"
      to: ".planning/ROADMAP.md"
      via: "the provisional re-derived PROD-12 target and Tier A entry gate feed Phase 4 scoping"
      pattern: "rederived"
---

<objective>
Run the SPIKE-01 sourcing spike: take three Tier A products fully end to end (historic formulation plus a MANUAL two-pass verification dry-run plus assigned driver), record the real per-product effort and the dead-end rate, and re-derive the launch-corpus target (PROD-12) and the Phase 4 Tier A entry gate as PROVISIONAL n=3 estimates. The three product records dogfood the schema gate, proving the data model works on real content. This is manual historic-sourcing and verification work, so it is autonomous false.

Purpose: The corpus scope is evidence-derived, not asserted upfront. This spike produces the first hard evidence and names what the Phase 2 automated verification gate must enforce.
Output: three real product records plus their timeline events, SPIKE-01-FINDINGS.md, spike-findings.json, its schema, and a test.

Scope honesty: in Phase 1 ONLY the structural claim-type shape runs as a gate (plan 01-05). The full two-pass verification workflow performed in this spike (distinct-lineage sourcing, at least one primary, citation-existence checks, disagreement/adjudication routing) is a MANUAL editorial dry-run recorded as findings here, and is automated only in Phase 2 (VRFY-01). A named deliverable of this spike is therefore "the requirements the Phase 2 automated gate must enforce".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-RESEARCH.md
@.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-VALIDATION.md
@./CLAUDE.md

<interfaces>
- schemas/product.schema.json, schemas/timeline-event.schema.json (from plan 01-04; the records must validate)
- schemas/sourced-value.schema.json, schemas/date-value.schema.json (the envelope and dates; checkedOn + claimDomain available)
- src/_data/sources.json (records must cite real registry source ids; add new sources as needed)
- docs/sourcing-backlog.json (from plan 01-09; the three spike products are drawn from this backlog of tiered candidates)
- scripts/validate-data.mjs (from plan 01-05; the gate the records must pass)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Source and two-pass verify three Tier A products end to end</name>
  <read_first>
    - ROADMAP.md "## Workstreams" Historic-sourcing track (the SPIKE-01 exit: three fully verified Tier A products plus a re-derived corpus/entry-gate figure plus the >=20 backlog) and PROJECT.md Constraints "Two-pass verification"
    - docs/sourcing-backlog.json (the candidate flagship targets to draw the three from)
    - schemas/product.schema.json, schemas/timeline-event.schema.json, schemas/sourced-value.schema.json (the shapes the records must satisfy)
  </read_first>
  <what-built>This task is the manual editorial spike: choosing three Tier A products from the backlog, hand-sourcing each historical formulation, running the two-pass verification the claim type demands AS A MANUAL DRY-RUN (corroborable facts: two distinct-lineage sources, at least one primary; authoritative facts: one authority plus an independent re-read), assigning the documented driver, and recording the real per-product effort, the number of ATTEMPTS, and the dead-end rate encountered. The two-pass workflow is NOT gate-enforced in Phase 1 (only the structural claim-type shape runs); it is recorded as findings and as the requirements the Phase 2 automated gate must enforce. Claude can assist with drafting and structuring, but the sourcing and verification are human editorial work.</what-built>
  <how-to-verify>
    1. Select three Tier A products from docs/sourcing-backlog.json spanning the flagship categories, noting how many candidates were ATTEMPTED to land three (the dead-end denominator).
    2. For each, source the historical and current formulation from primary/authoritative sources, recording each source in src/_data/sources.json with its jurisdiction and licence.
    3. Run the two-pass verification dry-run for each fact and record the verdicts.
    4. Note the per-product effort (time/passes), the attempts, and any dead ends (targets that could not be sourced to Tier A).
    5. Confirm the editorial sourcing is genuinely complete for all three before the records are written.
  </how-to-verify>
  <resume-signal>Type "sourced" once three products are fully sourced and two-pass verified (dry-run) with attempts, effort and dead-ends noted, or describe blockers.</resume-signal>
  <acceptance_criteria>Three Tier A products have been hand-sourced and two-pass verified as a manual dry-run, their sources recorded in the registry, and the per-product effort, the attempt count, and the dead-end rate noted, ready to encode as records and findings.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Encode the three product records and timeline events</name>
  <files>src/_data/products/spike-01.json, src/_data/products/spike-02.json, src/_data/products/spike-03.json, src/_data/timeline/spike-events.json</files>
  <read_first>
    - schemas/product.schema.json and schemas/timeline-event.schema.json (from plan 01-04)
    - src/_data/sources.json (the registry the records cite, updated in Task 1)
    - 01-RESEARCH.md section "## Architectural Responsibility Map" (the SPIKE-01 row: 3 real entity records dogfood the schema gate)
  </read_first>
  <action>
    Encode the three sourced products as src/_data/products/spike-01.json, spike-02.json and spike-03.json, each a valid product record: fact-bearing fields (current and historical formulation, manufacturer, nutrition where sourced, structured allergens) as SourcedValue envelopes citing the real registry source ids, with confidence and evidence from the GRADE enum, updated dates, the correct claimType (corroborable for empirical formulation facts with two distinct sources, authoritative for label/regulatory facts), and claimDomain set where relevant (regulatory fields carry claimDomain regulatory with a GB source and a checkedOn date). Encode the sourced formulation-change events in src/_data/timeline/spike-events.json as TimelineEvent records with ranged/circa dates, the three-way documentedChange / statedReason / labelledInference separation, and the assigned documented driver. Every record must pass scripts/validate-data.mjs (the prebuild gate). British English, no em-dashes, no imputation of motive as fact.
  </action>
  <verify>
    <automated>node scripts/validate-data.mjs && node -e "const fs=require('fs'); ['spike-01','spike-02','spike-03'].forEach(n=>{if(!fs.existsSync('src/_data/products/'+n+'.json'))process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>The three product records and the timeline events exist, cite real registry sources, and pass the validation gate (`node scripts/validate-data.mjs` exits 0). The records dogfood the full schema on real content.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Findings, provisional re-derived figures and the spike test</name>
  <files>docs/SPIKE-01-FINDINGS.md, docs/spike-findings.json, schemas/spike-findings.schema.json, test/spike.test.js</files>
  <behavior>
    - spike-findings.json records the number of ATTEMPTS and, per product, the outcome and whether it was sourced, plus per-product effort, the dead-end rate (consistent with attempts vs sourced), and the PROVISIONAL re-derived PROD-12 corpus target and Tier A entry gate.
    - test/spike.test.js asserts three sourced products, that the dead-end rate is CONSISTENT with the recorded attempts and outcomes (not merely present), the presence of the provisional re-derived figures, and that each spike product record validates against product.schema.json.
  </behavior>
  <read_first>
    - ROADMAP.md cross-phase data dependencies (SPIKE-01 re-derives PROD-12 and the Phase 4 Tier A entry gate; the provisional figures ~100 corpus and >=15 Tier A are placeholders the spike replaces, themselves provisional and refined in Phase 4)
    - 01-VALIDATION.md "## Manual-Only Verifications" (SPIKE-01 row: spike-findings.json test asserts 3 products with effort + dead-end + re-derived figures; the 3 records dogfood the gate)
    - src/_data/products/spike-0*.json (from Task 2)
  </read_first>
  <action>
    Create schemas/spike-findings.schema.json describing the findings: an attempts integer (the dead-end denominator); a products array of three entries each with an effort measure, an outcome and a sourced boolean; a deadEndRate field; and a rederived object with corpusTarget (the re-derived PROD-12 figure) and tierAEntryGate (the re-derived Phase 4 threshold). Create docs/spike-findings.json populated from the Task 1 evidence and the Task 2 records: record attempts and per-product outcome/sourced so deadEndRate has a denominator. Create docs/SPIKE-01-FINDINGS.md narrating the spike, the effort, the attempts and dead ends, the stated assumptions, and the reasoning behind the re-derived figures in plain en-GB; frame corpusTarget and tierAEntryGate explicitly as PROVISIONAL n=3 estimates, placeholders Phase 4 refines, NOT hard commitments, and record the method and per-product basis behind them. State that the two-pass verification was a manual dry-run and list the requirements the Phase 2 automated gate must enforce. Create test/spike.test.js asserting: exactly three sourced products in the findings, each with a recorded effort; an attempts denominator; a deadEndRate CONSISTENT with attempts and the per-product outcomes (compute and compare, do not just check presence); a rederived block with corpusTarget and tierAEntryGate present; and that each src/_data/products/spike-0*.json validates against product.schema.json (via lib/validate.mjs or a direct Ajv compile). No em-dashes. AUTHORING NOTE (rule-explaining prose): docs/SPIKE-01-FINDINGS.md is analyst-scope prose narrating reformulation findings; if the narrative must NAME a banned Class B term (e.g. when reporting a manufacturer's attributed stated reason), wrap that line in the `<!-- editorial-allow: quote -->` directive (or avoid the literal token), or the editorial gate (plan 01-06) will fail the build.
  </action>
  <verify>
    <automated>node --test test/spike.test.js</automated>
  </verify>
  <acceptance_criteria>test/spike.test.js passes: three sourced products with effort, an attempts denominator, a deadEndRate consistent with the recorded attempts/outcomes, and provisional re-derived corpus target and Tier A entry gate are recorded, and the three product records validate against the schema. SPIKE-01 is machine-verifiable, the corpus scope is evidence-derived (and honestly provisional), and the Phase 2 gate requirements are named.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hand-sourced historic data to canonical store | The first real content enters the dataset and must pass the same gates as all future content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | A fabricated or under-sourced historical formulation published as fact | mitigate | Two-pass verification dry-run in Task 1; the records pass the provenance and claim-type gate (plan 01-05); corroborable formulation facts require two distinct sources |
| T-10-02 | Tampering / Elevation | Motive imputed as fact in a change narrative (defamation) | mitigate | TimelineEvent keeps statedReason and labelledInference separate; the editorial lint (plan 01-06) bans motive verbs in analyst prose; no imputation of motive |
| T-10-03 | Repudiation | The re-derived corpus figures are asserted as settled, or the dead-end rate has no denominator | mitigate | spike-findings.json records attempts, per-product outcome and effort behind the figures; FINDINGS.md frames the re-derived figures as provisional n=3 placeholders; the test asserts the rate is consistent with the attempts, not merely present |
</threat_model>

<verification>
- `node scripts/validate-data.mjs` exits 0 with the three spike records present (they pass the gate).
- `node --test test/spike.test.js` passes (incl. the dead-end-rate consistency check).
- The provisional re-derived PROD-12 target and Tier A entry gate are recorded for Phase 4 scoping.
</verification>

<success_criteria>
Three Tier A products are sourced, two-pass verified (manual dry-run) and encoded as schema-valid records, the per-product effort, attempts and dead-end rate are recorded with a denominator, the launch-corpus target and Phase 4 Tier A entry gate are re-derived as provisional n=3 placeholders, and the spike names the requirements the Phase 2 automated gate must enforce (SPIKE-01).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-trust-primitives-schemas-rights-ci-deploy-sourcin/01-10-SUMMARY.md` when done.
</output>
