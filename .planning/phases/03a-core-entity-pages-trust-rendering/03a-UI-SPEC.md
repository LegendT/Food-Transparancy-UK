---
phase: 3a
slug: core-entity-pages-trust-rendering
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-01
---

# Phase 3a - UI Design Contract

> Visual and interaction contract for the product and ingredient pages, and for the reader-facing trust states (withheld, contested, stale, allergen safety). Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Governing idea:** this phase makes verification uncertainty legible to a non-expert. A shopper or a citing journalist must be able to tell at a glance what is verified, what is withheld, what is contested and what is due for re-verification. The colour is only ever a second cue; the text label always carries the meaning (WCAG 1.4.1, SITE-04). The mostly-withheld page is the PRIMARY case (D-13): a page of withheld placeholders must read as purposeful and trustworthy, not broken.

This spec EXTENDS the existing `sourcedValue` macro (`src/_includes/components/macros.njk`) and the base styles (`src/assets/styles.css`); it does not replace the Phase 2 derivation. Every treatment below consumes the `factState` projection (`{ state, publishable, stale, contested, value?, positions }`); no template re-derives status and none reads `fact.value` (R-31).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Eleventy 3.1.6 + Nunjucks + CUBE CSS; shadcn not applicable to a non-React SSG) |
| Preset | not applicable |
| Component library | none (vanilla, progressive enhancement, no new dependencies) |
| Icon library | none (text-first; the existing CSS-drawn disclosure chevron is the only glyph) |
| Font | `system-ui, -apple-system, "Segoe UI", Arial, sans-serif` (existing, unchanged) |

Registry safety gate is not applicable (no shadcn, no third-party registries).

---

## Spacing Scale

The project styles are rem-based on a 0.25rem (4px) step. All declared values are multiples of 4. Keep authoring in rem so the scale respects user font-size preferences.

| Token | Value | rem | Usage |
|-------|-------|-----|-------|
| xs | 4px | 0.25rem | Icon gap, token inline padding, chevron offset |
| sm | 8px | 0.5rem | Trust-token gaps, source-list spacing |
| xsm | 12px | 0.75rem | Disclosure top margin (existing) |
| md | 16px | 1rem | Default element padding, card padding |
| lg | 24px | 1.5rem | Fact block vertical rhythm, section padding (existing) |
| xl | 32px | 2rem | Between labelled product sections |
| 2xl | 48px | 3rem | Page-title to first section |

Exceptions: none. The 12px step (`0.75rem`) is inherited from the existing `.fact__detail` and `.fact__note` rules and is a multiple of 4; keep it rather than churning existing markup.

---

## Typography

Two weights only: regular 400 and bold 700 (matches the existing GOV.UK-derived styling; `.fact__label`, `.fact__value`, `summary` are already 700). Body line-height 1.5, headings 1.2.

| Role | Size | rem | Weight | Line Height | Usage |
|------|------|-----|--------|-------------|-------|
| Meta | 14px | 0.875rem | 400 | 1.5 | Source notes, dates, "review due", provenance detail |
| Body | 16px | 1rem | 400 | 1.5 | Prose, fact values, table cells, explainer text |
| Heading | 20px | 1.25rem | 700 | 1.2 | Section headings (`h2`: Ingredients, Nutrition, Allergens...), lede |
| Title | 28px | 1.75rem | 700 | 1.2 | Page title (`h1`: product / ingredient name) |

Exceptions: the existing `.lede` at 18px (1.125rem) may remain on the foundation pages; on the new entity pages the lede uses the 20px Heading size for a single consistent scale.

---

## Color

Palette is derived from the GOV.UK Design System colours already present in `styles.css` (near-black text, yellow focus, grey borders, light-grey panels). Every value below that carries TEXT meets 4.5:1 on white. The state colours are a functional (semantic) palette that sits outside the 60/30/10 brand split; they signal meaning and are always paired with a text label.

### Brand / surface split (60 / 30 / 10)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#ffffff` | Page background, fact-card and table surfaces |
| Secondary (30%) | `#f3f2f1` | Section panels, disclosure and caveat backgrounds, nutrition-table header/zebra |
| Accent (10%) | `#1d70b8` | Links, trust-token links, ingredient cross-links (contrast 4.6:1 on white) |
| Text | `#0b0c0c` | All body and heading text |
| Neutral border | `#b1b4b6` | Default fact-card and table borders (existing) |
| Focus | `#ffdd00` on `#0b0c0c` | Keyboard focus block (existing; never suppress) |

Accent reserved for: hyperlinks, the confidence/evidence trust-token links, the "Source and date" disclosure summary, and product-to-ingredient cross-links. Never applied to plain text, section headings, or as a general "make it pop" colour.

Note: the existing link colour in `styles.css` is `#1d4ed8`. Migrate links to `#1d70b8` (GOV.UK) for one consistent accent, or keep `#1d4ed8` (contrast 5.7:1, also compliant) if the executor prefers minimal churn. Pick one and apply site-wide; do not mix.

### Semantic state palette (meaning, always with a text label)

| State cue | Value | On white | Applied as | Never as |
|-----------|-------|----------|------------|----------|
| Withheld (ordinary) | `#505a5f` (GOV.UK secondary grey) | 7.0:1 | Left accent bar + label text | The only signal; label is mandatory |
| Stale / review-due | `#f47738` (GOV.UK orange) | 2.9:1 | 4px left accent bar ONLY (decorative) | Text colour (fails 4.5:1); the "review due" text stays `#0b0c0c` |
| Contested | `#1d70b8` | 4.6:1 | Left accent bar + "Contested" heading text | Red (contested is honest disagreement, not danger) |
| Allergen hazard / destructive | `#d4351c` (GOV.UK red) | 4.6:1 | Border, left bar, warning label text | Reassurance (allergens fail safe toward warning only) |
| Verified (optional affirm) | `#00703c` (GOV.UK green) | 4.9:1 | Optional small "Verified" affix text | The primary signal (the value and tokens already do that) |

**Why ordinary withheld is calm grey, not red (resolves D-01 vs D-13):** withheld is conspicuous through a consistent distinct card treatment and an explicit label, not through alarm colour. At launch most facts are withheld; a page of red would read as broken and would drain the red signal of meaning. Red is reserved strictly for the one genuinely dangerous case, allergen safety.

---

## Copywriting Contract

British English throughout. No em-dashes (the editorial lint fails the build; use commas, semicolons or hyphens). No emoji. Plain English, no jargon; the trust vocabulary is defined once at Methodology and linked, not re-explained on every page.

### Standard rows

| Element | Copy |
|---------|------|
| Primary action (product) | "Source and date" (the per-fact provenance disclosure summary, existing) |
| Primary action (cross-link) | "View this ingredient" (product ingredient list to ingredient page) |
| Empty state heading | "No recipe changes recorded yet" (recipe-history section, SC3) |
| Empty state body | "We have not yet sourced and verified any formulation changes for this product. When we do, they will appear here, each with its source and date." |
| Error / unavailable | "Not yet verified; withheld." (the withheld placeholder; existing floor string, kept verbatim) |
| Destructive confirmation | Not applicable (entity pages are read-only; there are no destructive actions in this phase) |

### Phase-critical trust-state copy (load-bearing; use verbatim)

| Situation | Copy |
|-----------|------|
| Withheld, collapsed label | "Not yet verified; withheld." |
| Withheld, expanded explanation | "This figure has not passed our two-pass verification, so we are not publishing a value. The sources we are checking are listed below." |
| Stale, inline | "Last verified {date}; review due." (existing floor string, kept verbatim) |
| Contested, heading | "Contested; sources disagree:" (existing floor string) |
| Contested, per-position | "{value} - {source name}{, note}" (each position carries its OWN source; F6 gap) |
| Recipe history absent | See empty-state rows above |

### Allergen copy (D-12, highest-stakes; overrides general treatment)

| Situation | Copy |
|-----------|------|
| Standing caveat (on ANY page showing allergens) | "Allergen information can change. Always check the current product packaging. Do not rely on this archive for allergy safety." |
| Verified present | "Contains {allergen}." |
| Verified may-contain | "May contain {allergen}." |
| Verified absent | "Not declared on the sourced label." (a statement about the label, never an absolute "does not contain") |
| Withheld absent (HAZARD) | "Not yet verified. We cannot confirm whether this product contains {allergen}. Check the pack." (MUST NEVER render as "does not contain {allergen}") |
| Withheld present / may-contain | "Possible {allergen}, not yet verified. Treat as present until confirmed. Check the pack." (MUST NEVER be hidden) |

### Ingredient page copy

| Element | Copy |
|---------|------|
| Regulatory-status block heading | "Current GB regulatory position" |
| Authority-position block heading (conditional, D-14) | "Named authority's safety opinion" |
| Standing not-advice note (both blocks) | "This describes a regulatory or authority position. It is not medical or dietary advice." |
| Products-containing heading (INGR-04) | "Products that list this ingredient" |
| Products-containing, best-effort caveat (only if D-15 resolves to name-matching) | "Matched by ingredient name against declared ingredient lists; this list is best-effort and may be incomplete." |

---

## State Treatment Matrix

The `factState` projection collapses the seven Phase 2 states into three render branches (`publishable`, `contested`, else `withheld`) plus a `stale` flag. Style the outer `.fact` by its `data-state` attribute (already emitted by the macro). All seven states must resolve to a defined treatment.

| Projection branch | Underlying states | Left accent bar | Card border | Background | Text label (mandatory) |
|-------------------|-------------------|-----------------|-------------|------------|------------------------|
| publishable, not stale | `published-confirmed` | none | `#b1b4b6` 2px (existing) | `#ffffff` | value + confidence/evidence tokens |
| publishable, stale | `published-stale` | `#f47738` 4px | `#b1b4b6` 2px | `#ffffff` | value + tokens + "Last verified {date}; review due." (black text) |
| contested | `published-contested` | `#1d70b8` 4px | `#1d70b8` 2px | `#f3f2f1` | "Contested; sources disagree:" + both-sides list |
| withheld | `withheld-unverified`, `withheld-in-review`, `withheld-open-disagreement`, `withheld-wrong` | `#505a5f` 4px | `#b1b4b6` 2px dashed | `#ffffff` | "Not yet verified; withheld." |

Add the left accent bar with `border-inline-start` so it flips correctly under logical properties. The dashed border on withheld is a colour-independent second cue that the card is a placeholder, not a value. Do not introduce distinct treatments per withheld sub-state; the reader is shown one honest "withheld" surface (the sub-state distinction is internal and lives in the disclosure text only if needed).

---

## Component Inventory

Reuse first; the only new primitives are the contested both-sides extension, the allergen list, and the nutrition table pattern.

| Component | Status | Contract |
|-----------|--------|----------|
| `sourcedValue` macro | EXTEND | Add the state accent bars (above). Extend the contested branch to resolve and render each position's own sources (F6): for `p in d.positions`, look up `p.sources` against the sources array and render them, so a contested fact shows both sides WITH provenance, not bare values. |
| `caveatBox` macro | REUSE | Use for the standing allergen-safety caveat and the ingredient not-advice note. |
| `sourceNote` macro | REUSE | Page-level Sources roll-up (D-07, PROD-04). |
| Allergen list | NEW | See Allergen render rules. A definition-style list, one row per declared allergen, each carrying presence + provenance via `factState` on `allergen.provenance`. |
| Nutrition table | NEW | See Nutrition table pattern. Table for scannable values plus an anchored per-figure provenance list. |
| Product page template | NEW | `h1` name, then labelled `h2` sections in this order: Ingredients, Nutrition, Allergens, Manufacturer, Sources, Recipe history (D-05). |
| Ingredient page template | NEW | `h1` name + synonyms + E-number + function explainer; then the GB regulatory block; then the optional authority block (D-14); then products-containing (D-15). |

---

## Nutrition Table Pattern (resolves D-06 / F4)

The `sourcedValue` block macro must not be dropped into each `<td>`: most of the nine figures are absent in real data (spike-02 carries only `sugars`), and a block-with-disclosure per cell destroys table scannability. **Prescribed pattern: a scannable value table plus an anchored per-figure provenance list below it.** This reuses the existing block macro unchanged, stays fully no-JS accessible, and cleanly separates the three cell conditions.

Structure:

- A `<table>` with a `<caption>` "Nutrition per 100g" and a proper `<thead>` (`scope="col"`). Columns: Nutrient | Per 100g | Status.
- One `<tr>` per nutrient, iterating the nine fixed keys in schema order (energyKj, energyKcal, fat, saturates, carbohydrate, sugars, fibre, protein, salt), with a `<th scope="row">` nutrient name.
- The "Per 100g" and "Status" cells are driven by `factState` on each figure:

| Cell condition | Per 100g cell | Status cell |
|----------------|---------------|-------------|
| Key ABSENT from `nutrition` object (not recorded) | "Not recorded" (muted, `#505a5f`) | empty / "-" |
| Key present, `publishable` | value + unit (e.g. "56g") | link "Verified, see source" to the figure's provenance anchor |
| Key present, `publishable` + `stale` | value + "review due" | link to provenance anchor |
| Key present, withheld | "Not yet verified" (withheld treatment inline) | link "Sources being checked" to provenance anchor |
| Key present, contested | "Contested" | link to the both-sides block in the provenance list |

- Below the table, a "Nutrition sources" region renders the FULL `sourcedValue` macro block for EACH recorded figure, wrapped in an element with `id="nutrition-{key}"` so the table status links resolve to it. Absent figures produce no provenance block.

This makes the three conditions unambiguous to a reader: "Not recorded" (we never had this figure) is visibly different from "Not yet verified" (we have it but it has not passed the gate) which is different from a published value. Do not conflate them.

---

## Allergen Render Rules (D-12, highest-stakes; overrides general treatment)

Allergens fail safe TOWARD warning, never toward reassurance. This section overrides the general State Treatment Matrix wherever they conflict.

- The standing allergen-safety caveat (copy above) renders via `caveatBox` at the top of the Allergens section on every page that shows allergens, present or not.
- One row per declared allergen. Presence AND its provenance state are both shown; presence is derived through `factState` on `allergen.provenance` so no unverified determination leaks as fact (R-31 applies here too).

| presence | provenance state | Treatment | Colour cue |
|----------|------------------|-----------|------------|
| present | publishable | "Contains {allergen}." + provenance disclosure | `#d4351c` warning bar (a real allergen present is safety-relevant) |
| may-contain | publishable | "May contain {allergen}." + disclosure | `#d4351c` warning bar |
| absent | publishable | "Not declared on the sourced label." | neutral `#505a5f`, no reassurance styling |
| present or may-contain | withheld | "Possible {allergen}, not yet verified. Treat as present until confirmed. Check the pack." (NEVER hidden) | `#d4351c` warning bar |
| absent | withheld | "Not yet verified. We cannot confirm whether this product contains {allergen}. Check the pack." (NEVER "does not contain") | `#d4351c` warning bar (uncertainty about absence is treated as risk) |

Hard invariants for the checker and executor:
1. A withheld `absent` claim MUST NOT render any wording that implies the allergen is absent.
2. A withheld `present` / `may-contain` claim MUST NOT be hidden or collapsed away.
3. Red is a second cue only; every allergen row states its meaning in text.

---

## Ingredient Page Rules (D-08 / D-09 / D-14 / D-15)

- The GB regulatory-status block and the optional authority-position block are two distinct, clearly-headed, cited and dated blocks, visually separate from the plain-English "what it is / why it is used" explainer, so a safety opinion is never misread as the regulatory status.
- Both blocks carry the standing not-advice note (copy above). Keep this LOCALISED; the site-wide "not medical or dietary advice" disclaimer is SITE-09 in Phase 3b. Complementary, not duplicated.
- The authority-position block (INGR-02) renders ONLY if its field is present. D-14 is a blocking schema decision the plan settles first (recommended: a distinct `authorityPosition` fact-bearing field, never folded into `regulatoryStatus`). Until the field exists, the block does not render; the page must not show an empty stub.
- The products-containing list (INGR-04) depends on D-15 (recommended: a structured `ingredients: [ingredientId]` field on the product schema). If the plan instead resolves to free-text name matching, the best-effort caveat copy above is mandatory.
- A single study appears only as labelled illustration, never as an evidence statement (the no-synthesis boundary).

---

## Recipe History Absent State (SC3, Claude's discretion within D-01)

When a product has no sourced change events, the recipe-history section renders as an honest empty state (heading + body copy above), styled as a calm `#f3f2f1` panel, NOT as a withheld/error card and NOT omitted. "Absent, not a broken stub." The conditional "full timeline" link (Phase 8) renders only when a timeline page exists.

---

## Accessibility Floor (WCAG 2.2 AA, hard)

- Every state label is text; colour is never the sole cue (1.4.1). Verified against the semantic palette table above.
- All interactive targets at least 44px (existing `.fact__token`, `summary`, nav already comply); new links and the nutrition status links inherit this.
- Visible focus is the existing yellow GOV.UK block; never suppressed.
- Nutrition uses a real `<table>` with `<caption>`, `<thead>`, `scope` attributes; the allergen list uses list semantics; sections use `h2` headings in a logical order under one `h1`.
- Contrast: all text-bearing colours listed meet 4.5:1 on white; `#f47738` is confined to a decorative border and never carries text.
- No-JS baseline: every disclosure is native `<details>`; the table-to-provenance link is a plain in-page anchor. Nothing here requires client JavaScript.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none | not applicable (no shadcn, no third-party registries) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
